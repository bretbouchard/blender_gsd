---
phase: 03-arm-physics
plan: 01
type: execute
wave: 1
depends_on: ["02-02"]
files_modified:
  - projects/tile-platform/lib/arms/__init__.py
  - projects/tile-platform/lib/arms/types.py
  - projects/tile-platform/lib/arms/joints.py
  - projects/tile-platform/lib/arms/physics.py
autonomous: true

must_haves:
  truths:
    - "Arms have multiple joints with configurable constraints"
    - "Physics simulation provides natural movement"
    - "Arms always reach target positions despite physics"
    - "Joint angles are limited to realistic ranges"
  artifacts:
    - path: "projects/tile-platform/lib/arms/types.py"
      provides: "Arm physics types"
      min_lines: 50
    - path: "projects/tile-platform/lib/arms/joints.py"
      provides: "Joint definitions"
      min_lines: 40
    - path: "projects/tile-platform/lib/arms/physics.py"
      provides: "Physics simulation"
      min_lines: 60
  key_links:
    - from: "lib/arms/physics.py"
      to: "lib/arms/joints.py"
      via: "composition"
      pattern: "self\\.joints\\s*=\\s*List\\[Joint\\]"
---

<objective>
Create arm physics system with multi-joint arms and constraints.

Purpose: Enable natural physics-driven arm movement with guaranteed target reach.
Output: Working arm physics classes.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@projects/tile-platform/.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Create arm physics types</name>
  <files>projects/tile-platform/lib/arms/types.py</files>
  <action>
Create arm physics type definitions:
1. Define enums:
   - JointType: HINGE, BALL, PRISMATIC, FIXED
   - ArmState: IDLE, EXTENDING, PLACING, RETRACTING
   - PhysicsMode: DYNAMIC, KINEMATIC, HYBRID

2. Define dataclasses:
   - JointConfig:
     - joint_type: JointType
     - min_angle: float
     - max_angle: float
     - damping: float
     - stiffness: float

   - ArmSegmentConfig:
     - length: float
     - width: float
     - height: float
     - mass: float

   - ArmPhysicsConfig:
     - mode: PhysicsMode
     - gravity: float
     - air_resistance: float
     - target_reach_force: float

3. All classes should use @dataclass decorator
4. Use standard library only (no bpy yet)
  </action>
  <verify>python -c projects/tile-platform/lib/arms/types.py</verify>
  <done>JointType, ArmState, PhysicsMode enums, JointConfig, ArmSegmentConfig, ArmPhysicsConfig classes exist</done>
</task>

<task type="auto">
  <name>Create joint system</name>
  <files>projects/tile-platform/lib/arms/joints.py</files>
  <action>
Create joint definitions and constraints:
1. Define Joint class with:
   - config: JointConfig
   - current_angle: float
   - target_angle: float
   - velocity: float
   - acceleration: float

2. Implement methods:
   - update(dt: float, target_angle: float) -> float
     * Returns new angle after physics update
     * Applies damping and stiffness
     * Clamps to min/max angle

   - get_torque() -> float
     * Calculates torque to reach target
     * Returns force needed to achieve target

   - is_at_limit() -> bool
     * Returns True if angle is at joint limits

3. Define JointChain class:
   - joints: List[Joint]
   - forward_kinematics(from_base: bool = True) -> List[float]
   - inverse_kinematics(target_angles: List[float]) -> List[float]
  </action>
  <verify>python -c projects/tile-platform/lib/arms/joints.py</verify>
  <done>Joint and JointChain classes with kinematics calculations</done>
</task>

<task type="auto">
  <name>Create physics simulation</name>
  <files>projects/tile-platform/lib/arms/physics.py</files>
  <action>
Create physics simulation system:
1. Define PhysicsSimulator class with:
   - config: ArmPhysicsConfig
   - time_step: float
   - joints: List[Joint]

2. Implement methods:
   - step(joints: List[Joint], target_angles: List[float]) -> List[float]
     * Performs one physics simulation step
     * Returns new joint angles
     * Uses Euler integration

   - apply_constraints(angles: List[float]) -> List[float]
     * Applies joint limits and constraints
     * Ensures angles stay within min/max

   - calculate_end_effector_position(joint_angles: List[float]) -> Tuple[float, float, float]
     * Forward kinematics from joint angles to end effector position
     * Returns (x, y, z) position

3. Implement constraint solver:
   - solve_for_target(target_pos: Tuple[float, float, float], current_angles: List[float]) -> List[float]
     * Inverse kinematics to find joint angles for target
     * Uses iterative solver with damping
     * Returns joint angles that reach target

Do NOT use bpy - pure Python physics simulation.
  </action>
  <verify>python -c projects/tile-platform/lib/arms/physics.py</verify>
  <done>PhysicsSimulator with step, constraint solver, and end effector calculation</done>
</task>

<task type="auto">
  <name>Create package exports</name>
  <files>projects/tile-platform/lib/arms/__init__.py</files>
  <action>
Create package exports for arms module:
1. Export all public classes and enums:
   - from .types import JointType, ArmState, PhysicsMode, JointConfig, ArmSegmentConfig, ArmPhysicsConfig
   - from .joints import Joint, JointChain
   - from .physics import PhysicsSimulator

2. Define __all__ list

3. Add module docstring
  </action>
  <verify>python -c "from projects.tile-platform.lib.arms import JointType, ArmState, PhysicsMode, PhysicsSimulator"</verify>
  <done>All arm classes and enums are exported from package</done>
</task>

</tasks>

<verification>
1. Joint types are correctly defined
2. Physics simulation produces natural movement
3. Constraints prevent impossible arm configurations
4. End effector position calculation is accurate
5. All code is testable without Blender
</verification>

<success_criteria>
- Arms have multiple joints with configurable constraints
- Physics simulation provides natural movement
- Arms always reach target positions despite physics
- Joint angles are limited to realistic ranges
- System is testable without Blender
</success_criteria>

<output>
After completion, create `.planning/phases/03-arm-physics/03-01-SUMMARY.md`
</output>
