---
phase: 11-gn-arms
plan: 01
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - projects/tile-platform/lib/gn_platform/arm_nodes.py
  - projects/tile-platform/lib/gn_platform/physics_nodes.py
  - projects/tile-platform/lib/gn_platform/__init__.py
autonomous: true

must_haves:
  truths:
    - "Arm physics uses SimulationBuilder zones"
    - "Multi-joint state is stored in simulation state variables"
    - "Constraints are enforced via GN math nodes"
    - "Target reach is guaranteed within tolerance"
  artifacts:
    - path: "projects/tile-platform/lib/gn_platform/physics_nodes.py"
      provides: "Physics simulation using SimulationBuilder"
      min_lines: 120
    - path: "projects/tile-platform/lib/gn_platform/arm_nodes.py"
      provides: "ArmGN class with multi-joint support"
      min_lines: 150
  key_links:
    - from: "lib/gn_platform/physics_nodes.py"
      to: "lib/geometry_nodes/simulation.py"
      via: "import"
      pattern: "from lib.geometry_nodes import SimulationBuilder"
---

<objective>
Migrate arm physics system to Geometry Nodes using SimulationBuilder.

Purpose: Replace custom Python physics with proper GN simulation zones.
Output: Working GN-based arm physics with constraints.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/geometry_nodes/simulation.py
@lib/arms/physics.py
@lib/arms/arm.py
</context>

<tasks>

<task type="auto">
  <name>Create PhysicsGN with SimulationBuilder</name>
  <files>projects/tile-platform/lib/gn_platform/physics_nodes.py</files>
  <action>
Create the PhysicsGN class using SimulationBuilder:
1. Define PhysicsGN class:
   - builder: NodeTreeBuilder reference
   - sim: SimulationBuilder instance
   - config: ArmPhysicsConfig

2. Implement simulation zone creation:
   - create_physics_zone() method
   - State variables:
     * position: VECTOR (default 0, 0, 0)
     * velocity: VECTOR (default 0, 0, 0)
     * angle: VALUE (default 0.0)
     * angular_velocity: VALUE (default 0.0)

3. Implement physics update step:
   - _physics_update(zone, dt) method
   - Apply gravity: (0, 0, -9.81)
   - Integrate velocity: new_vel = vel + gravity * dt
   - Integrate position: new_pos = pos + vel * dt
   - Calculate torque from target
   - Integrate angular velocity and angle

4. Implement constraint enforcement:
   - _apply_constraints(zone) method
   - Target reach constraint (max_reach)
   - Joint angle limits (min_angle, max_angle)

5. Reference Python implementation at `lib/arms/physics.py`

CRITICAL: Must use SimulationBuilder, not custom Python simulation
  </action>
  <verify>python -c "from projects.tile-platform.lib.gn_platform import PhysicsGN; print('OK')"</verify>
  <done>PhysicsGN creates simulation zone with state variables</done>
</task>

<task type="auto">
  <name>Create ArmGN with multi-joint support</name>
  <files>projects/tile-platform/lib/gn_platform/arm_nodes.py</files>
  <action>
Create the ArmGN class for complete arm assemblies:
1. Define ArmGN class:
   - builder: NodeTreeBuilder reference
   - segments: List[ArmSegmentConfig]
   - joints: List[JointChain]
   - physics: PhysicsGN

2. Implement arm geometry generation:
   - build_arm_geometry() method
   - Create segment meshes
   - Apply joint rotations
   - Forward kinematics chain

3. Implement multi-joint system:
   - create_joint_chain() method
   - Each joint has:
     * Hinge rotation around Z axis
     * Angle limits (min, max)
     * Damping factor

4. Implement IK solver (simplified):
   - solve_for_target(target_pos) method
   - Iterative CCD solver
   - Returns joint angles

5. Implement update loop:
   - update(dt, target_pos) method
   - Run physics simulation step
   - Apply IK solution
   - Update geometry transforms

6. Reference Python implementation at `lib/arms/arm.py`

CRITICAL: All physics must go through SimulationBuilder
  </action>
  <verify>python -c "from projects.tile-platform.lib.gn_platform import ArmGN; print('OK')"</verify>
  <done>ArmGN creates multi-joint arm with physics</done>
</task>

<task type="auto">
  <name>Update package exports</name>
  <files>projects/tile-platform/lib/gn_platform/__init__.py</files>
  <action>
Update __init__.py to include new classes:
1. Add imports:
   - from .physics_nodes import PhysicsGN
   - from .arm_nodes import ArmGN

2. Update __all__ list

3. Add usage example in docstring
  </action>
  <verify>python -c "from projects.tile-platform.lib.gn_platform import PhysicsGN, ArmGN"</verify>
  <done>PhysicsGN and ArmGN exported from package</done>
</task>

</tasks>

<verification>
1. PhysicsGN uses SimulationBuilder (not custom Python)
2. State variables are properly defined in simulation zone
3. Gravity and torque calculations match Python reference
4. Constraints are enforced after physics step
5. ArmGN creates valid multi-joint geometry
6. IK solver produces correct joint angles
</verification>

<success_criteria>
- Arm physics uses SimulationBuilder zones
- Multi-joint state is stored in simulation state variables
- Constraints are enforced via GN math nodes
- Target reach is guaranteed within tolerance (0.1)
- Physics output matches Python reference implementation
</success_criteria>

<output>
After completion, create `.planning/phases/11-gn-arms/11-01-SUMMARY.md`
</output>
