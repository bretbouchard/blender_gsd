---
phase: 08-export-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - projects/tile-platform/lib/export/render.py
  - projects/tile-platform/lib/export/__init__.py
autonomous: true

must_haves:
  truths:
    - "Platform renders high-quality animation in Blender"
    - "Render pipeline is optimized for production"
    - "Output is production-quality"
    - "Animation can be rendered in multiple formats"
  artifacts:
    - path: "projects/tile-platform/lib/export/render.py"
      provides: "RenderPipeline"
      min_lines: 60
  key_links:
    - from: "lib/export/render.py"
      to: "lib/foundation/platform.py"
      via: "import"
      pattern: "from\\.\\.foundation import Platform"
---

<objective>
Create render pipeline for high-quality Blender animation.

Purpose: Enable production-quality rendering of platform animations.
Output: Working RenderPipeline class.
</objective>

<tasks>

<task type="auto">
  <name>Create RenderPipeline</name>
  <files>projects/tile-platform/lib/export/render.py</files>
  <action>
Create RenderPipeline for production rendering:
1. Define RenderConfig dataclass:
   - resolution: Tuple[int, int] (default (1920, 1080))
   - frame_rate: int (default 24)
   - samples: int (default 128)
   - denoiser: str (default "optix")

2. Define RenderPipeline class with:
   - platform: Platform
   - config: RenderConfig

3. Implement methods:
   - render_animation(output_path: str, start_frame: int, end_frame: int) -> bool
     * Renders animation to file
     * Returns True on success

   - render_frame(frame_number: int, output_path: str) -> bool
     * Renders single frame
     * Returns True on success

   - set_render_engine(engine: str) -> None
     * Sets render engine ("cycles", "eevee")

   - get_render_stats() -> Dict[str, Any]
     * Returns render statistics
     * {time_per_frame, total_time, output_size}

4. Optimization features:
   - Motion blur support
   - Depth of field
   - Ambient occlusion
   - Compositor integration
  </action>
  <verify>python -c projects/tile-platform/lib/export/render.py</verify>
  <done>RenderPipeline produces high-quality animation renders</done>
</task>

<task type="auto">
  <name>Update package exports</name>
  <files>projects/tile-platform/lib/export/__init__.py</files>
  <action>
Update package exports:
1. Add to imports:
   - from .render import RenderPipeline, RenderConfig

2. Add to __all__ list
  </action>
  <verify>python -c "from projects.tile-platform.lib.export import RenderPipeline, RenderConfig"</verify>
  <done>RenderPipeline is exported from export package</done>
</task>

</tasks>

<verification>
1. RenderPipeline produces valid animation files
2. Output is production-quality
3. Render times are reasonable
4. Multiple output formats supported (MP4, EXR sequence)
5. Compositor integration works
</verification>

<success_criteria>
- Platform renders high-quality animation in Blender
- Render pipeline is optimized for production
- Output is production-quality
- Animation can be rendered in multiple formats
</success_criteria>

<output>
After completion, create `.planning/phases/08-export-pipeline/08-02-SUMMARY.md`
</output>
