---
phase: 08-export-pipeline
plan: 01
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - projects/tile-platform/lib/export/__init__.py
  - projects/tile-platform/lib/export/fbx.py
  - projects/tile-platform/lib/export/gltf.py
autonomous: true

must_haves:
  truths:
    - "Platform exports to FBX format for Unity"
    - "Platform exports in glTF format for Unreal"
    - "Exported armatures are optimized for game engines"
    - "Export preserves tile and arm hierarchy"
  artifacts:
    - path: "projects/tile-platform/lib/export/fbx.py"
      provides: "FBXExporter"
      min_lines: 60
    - path: "projects/tile-platform/lib/export/gltf.py"
      provides: "GLTFExporter"
      min_lines: 60
  key_links:
    - from: "lib/export/fbx.py"
      to: "lib/foundation/platform.py"
      via: "import"
      pattern: "from\\.\\.foundation import Platform"
---

<objective>
Create export pipeline for game engines (FBX/glTF).

Purpose: Enable export of platform to Unity and Unreal Engine.
Output: Working FBX and glTF exporters.
</objective>

<tasks>

<task type="auto">
  <name>Create FBXExporter</name>
  <files>projects/tile-platform/lib/export/fbx.py</files>
  <action>
Create FBXExporter for Unity export:
1. Define FBXExportConfig dataclass:
   - include_armatures: bool (default True)
   - include_physics: bool (default False)
   - mesh_decimation: float (default 0.0)
   - texture_resolution: int (default 1024)

2. Define FBXExporter class with:
   - platform: Platform
   - config: FBXExportConfig

3. Implement methods:
   - export_to_file(filepath: str) -> bool
     * Exports platform to FBX file
     * Includes armatures for arm animation
     * Optimizes mesh for game engines

   - export_armature(arm: Arm) -> bytes
     * Exports single arm as FBX armature
     * Optimizes bone hierarchy

   - get_export_stats() -> Dict[str, Any]
     * Returns vertex count, face count, bone count
     * File size estimate

4. FBX-specific optimizations:
   - Limit bone count per arm (4-6 bones)
   - Merge similar vertices
   - Apply LOD if configured
  </action>
  <verify>python -c projects/tile-platform/lib/export/fbx.py</verify>
  <done>FBXExporter exports platform to FBX format</done>
</task>

<task type="auto">
  <name>Create GLTFExporter</name>
  <files>projects/tile-platform/lib/export/gltf.py</files>
  <action>
Create GLTFExporter for Unreal export:
1. Define GLTFExportConfig dataclass:
   - include_armatures: bool (default True)
   - embed_textures: bool (default True)
   - draco_compression: bool (default True)

2. Define GLTFExporter class with:
   - platform: Platform
   - config: GLTFExportConfig

3. Implement methods:
   - export_to_file(filepath: str) -> bool
     * Exports platform to glTF file
     * Uses Draco compression for smaller files
     * Embeds textures if configured

   - export_as_glb() -> bytes
     * Exports as binary glTF (.glb)
     * Returns bytes for direct use

   - get_export_stats() -> Dict[str, Any]
     * Returns vertex count, face count, texture count
     * File size estimate

4. glTF-specific optimizations:
   - Draco mesh compression
   - Texture optimization
   - Material batching
  </action>
  <verify>python -c projects/tile-platform/lib/export/gltf.py</verify>
  <done>GLTFExporter exports platform to glTF format</done>
</task>

<task type="auto">
  <name>Create package exports</name>
  <files>projects/tile-platform/lib/export/__init__.py</files>
  <action>
Create package exports:
1. Export all public classes:
   - from .fbx import FBXExporter, FBXExportConfig
   - from .gltf import GLTFExporter, GLTFExportConfig

2. Define __all__ list
  </action>
  <verify>python -c "from projects.tile-platform.lib.export import FBXExporter, GLTFExporter"</verify>
  <done>FBXExporter and GLTFExporter are exported</done>
</task>

</tasks>

<verification>
1. FBXExporter produces valid FBX files
2. FBX files import correctly in Unity
3. GLTFExporter produces valid glTF files
4. glTF files import correctly in Unreal
5. Exported armatures have reasonable bone counts
</verification>

<success_criteria>
- Platform exports to FBX format for Unity
- Platform exports to glTF format for Unreal
- Exported armatures are optimized for game engines
- Export preserves tile and arm hierarchy
- Files import without errors in target engines
</success_criteria>

<output>
After completion, create `.planning/phases/08-export-pipeline/08-01-SUMMARY.md`
</output>
