---
phase: 01-platform-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/tile-platform/lib/foundation/__init__.py
  - projects/tile-platform/lib/foundation/platform.py
  - projects/tile-platform/lib/foundation/grid.py
  - projects/tile-platform/lib/foundation/types.py
autonomous: true

must_haves:
  truths:
    - "Platform class exists and manages tile collection"
    - "Grid class tracks tile positions in 2D space"
    - "Tiles can be placed at specific coordinates"
    - "System integrates with Blender scene"
  artifacts:
    - path: "projects/tile-platform/lib/foundation/platform.py"
      provides: "Platform core class"
      min_lines: 50
    - path: "projects/tile-platform/lib/foundation/grid.py"
      provides: "Grid tracking system"
      min_lines: 40
    - path: "projects/tile-platform/lib/foundation/types.py"
      provides: "Type definitions"
      min_lines: 30
  key_links:
    - from: "lib/foundation/platform.py"
      to: "lib/foundation/grid.py"
      via: "composition"
      pattern: "self\\.grid\\s*=\\s*Grid"
    - from: "lib/foundation/grid.py"
      to: "lib/foundation/types.py"
      via: "import"
      pattern: "from\\.types import"
---

<objective>
Create the foundational module structure with Platform, Tile, and Grid classes.

Purpose: Establish core architecture for the tile platform system.
Output: Working foundation classes with basic tile management.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Create foundation types and enums</name>
  <files>projects/tile-platform/lib/foundation/types.py</files>
  <action>
Create type definitions for the platform system:
1. Define TileConfig dataclass with:
   - position: Tuple[float, float] (x, y coordinates)
   - shape: TileShape enum (SQUARE, OCTAGONAL, HEXAGONAL)
   - size: float (default 1.0)
   - material_settings: Dict[str, Any] (optional material properties)

2. Define TileShape enum with SQUARE, OCTAGONAL, HEXAGONAL values

3. Define PlatformConfig dataclass with:
   - initial_size: Tuple[int, int] (width, height in tiles)
   - tile_size: float (default 1.0)
   - tile_shape: TileShape (default SQUARE)
   - arm_count: int (default 4)

4. Add validation in __post_init__ for:
   - size > 0
   - arm_count >= 1

Do NOT use bpy imports at this stage - keep it pure Python for testability.
  </action>
  <verify>python -c projects/tile-platform/lib/foundation/types.py</verify>
  <done>TileConfig, TileShape, PlatformConfig classes exist with validation</done>
</task>

<task type="auto">
  <name>Create Grid tracking system</name>
  <files>projects/tile-platform/lib/foundation/grid.py</files>
  <action>
Create the Grid class for tracking tile positions:
1. Define Grid class with:
   - tiles: Dict[Tuple[int, int], TileConfig] - stores tiles by grid position
   - bounds: Tuple[int, int, int, int] - (min_x, min_y, max_x, max_y)

2. Implement methods:
   - add_tile(pos: Tuple[int, int], tile: TileConfig) -> bool
     * Returns False if position occupied
   - remove_tile(pos: Tuple[int, int]) -> Optional[TileConfig]
     * Returns removed tile or None
   - get_tile(pos: Tuple[int, int]) -> Optional[TileConfig]
   - get_neighbors(pos: Tuple[int, int]) -> List[Tuple[int, int]]
     * Returns adjacent positions (up, down, left, right)
   - get_bounds() -> Tuple[int, int, int, int]
     * Returns current platform extent

3. Use standard library only (no bpy, no numpy required)

Do NOT implement any Blender-specific code.
  </action>
  <verify>python -c projects/tile-platform/lib/foundation/grid.py</verify>
  <done>Grid class tracks tiles, supports add/remove/query operations</done>
</task>

<task type="auto">
  <name>Create Platform core class</name>
  <files>projects/tile-platform/lib/foundation/platform.py</files>
  <action>
Create the Platform class that manages the tile system:
1. Define Platform class with:
   - config: PlatformConfig
   - grid: Grid
   - arms: List[ArmConfig] (placeholder for Phase 3)

2. Implement methods:
   - __init__(config: PlatformConfig)
     * Initialize grid with initial_size tiles if provided
   - place_tile(grid_pos: Tuple[int, int], shape: Optional[TileShape] = None) -> bool
     * Add tile at position, returns success
   - remove_tile(grid_pos: Tuple[int, int]) -> bool
     * Remove tile from position, returns success
   - get_tile_positions() -> List[Tuple[int, int]]
     * Return all current tile positions
   - get_bounds() -> Tuple[int, int, int, int]
     * Return platform extent

3. Create ArmConfig placeholder dataclass with:
   - position: Tuple[int, int] (grid position)
   - status: str (default "idle")

Do NOT implement arm mechanics yet - that's Phase 3.
  </action>
  <verify>python -c projects/tile-platform/lib/foundation/platform.py</verify>
  <done>Platform class manages tiles, uses Grid for tracking</done>
</task>

<task type="auto">
  <name>Create package exports</name>
  <files>projects/tile-platform/lib/foundation/__init__.py</files>
  <action>
Create package exports for foundation module:
1. Export all public classes and enums:
   - from .types import TileConfig, TileShape, PlatformConfig
   - from .grid import Grid
   - from .platform import Platform, ArmConfig

2. Define __all__ list with all exports

3. Add version constant: __version__ = "0.1.0"
  </action>
  <verify>python -c "from projects.tile-platform.lib.foundation import Platform, Grid, TileConfig, TileShape, PlatformConfig, ArmConfig"</verify>
  <done>All foundation classes and enums are importable from package</done>
</task>

</tasks>

<verification>
1. All Python files have valid syntax (python -c check)
2. Classes are instantiable without errors
3. Grid correctly tracks tile positions
4. Platform manages tile lifecycle
5. No Blender dependencies in foundation layer
</verification>

<success_criteria>
- Platform class exists and manages tile collection
- Grid class tracks tile positions in 2D space
- Tiles can be placed at specific coordinates
- System is testable without Blender
- All classes have proper type hints
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation/01-01-SUMMARY.md`
</output>
