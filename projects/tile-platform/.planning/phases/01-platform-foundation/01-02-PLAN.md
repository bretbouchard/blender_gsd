---
phase: 01-platform-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - projects/tile-platform/lib/foundation/tile.py
  - projects/tile-platform/lib/foundation/platform.py
  - projects/tile-platform/lib/foundation/__init__.py
autonomous: true

must_haves:
  truths:
    - "Tiles have geometry that can be generated procedurally"
    - "Tiles support multiple shapes (square, octagonal, hexagonal)"
    - "Tile geometry is Blender-independent for testing"
    - "Platform can place and remove tiles"
  artifacts:
    - path: "projects/tile-platform/lib/foundation/tile.py"
      provides: "Tile geometry generation"
      min_lines: 60
    - path: "projects/tile-platform/lib/foundation/platform.py"
      provides: "place_tile and remove_tile methods"
      min_lines: 80
  key_links:
    - from: "lib/foundation/platform.py"
      to: "lib/foundation/tile.py"
      via: "import"
      pattern: "from\\.tile import"
---

<objective>
Implement tile geometry generation and platform tile management methods.

Purpose: Enable the platform to place and remove tiles with procedural geometry.
Output: Working tile placement and removal system.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Create Tile geometry generator</name>
  <files>projects/tile-platform/lib/foundation/tile.py</files>
  <action>
Create the Tile class for procedural tile geometry:
1. Define TileGeometry dataclass with:
   - vertices: List[Tuple[float, float, float]]
   - faces: List[Tuple[int, ...]]
   - normals: List[Tuple[float, float, float]]
   - uvs: Optional[List[Tuple[float, float]]]

2. Implement geometry generation methods:
   - generate_square(size: float) -> TileGeometry
   - generate_octagon(size: float) -> TileGeometry
   - generate_hexagon(size: float) -> TileGeometry

3. Each shape should:
   - Center at origin (0, 0, 0)
   - Have correct winding (CCW)
   - Include UV coordinates for texturing

4. Use standard library only - numpy is acceptable but not required for simple shapes.

Do NOT use bpy - this must work in pure Python for testing.
  </action>
  <verify>python -c projects/tile-platform/lib/foundation/tile.py</verify>
  <done>TileGeometry dataclass exists, can generate square, octagon, hexagon shapes</done>
</task>

<task type="auto">
  <name>Add tile placement methods to Platform</name>
  <files>projects/tile-platform/lib/foundation/platform.py</files>
  <action>
Extend the Platform class with tile management:
1. Add place_tile() method:
   - Parameters: position (Tuple[int, int]), shape (TileShape)
   - Creates TileGeometry based on shape
   - Stores in Grid
   - Returns bool (success/failure)
   - Raises ValueError if position occupied

2. Add remove_tile() method:
   - Parameters: position (Tuple[int, int])
   - Removes tile from Grid
   - Returns removed TileGeometry or None

3. Add get_tile_geometry() method:
   - Parameters: position (Tuple[int, int])
   - Returns TileGeometry at position or None

4. Add get_all_tiles() method:
   - Returns Dict[Tuple[int, int], TileGeometry]
   - Used for rendering and export

5. Keep track of placed tiles for later arm placement.
  </action>
  <verify>python -c projects/tile-platform/lib/foundation/platform.py</verify>
  <done>Platform can place and remove tiles, retrieve tile geometry</done>
</task>

<task type="auto">
  <name>Update package exports</name>
  <files>projects/tile-platform/lib/foundation/__init__.py</files>
  <action>
Update package exports to include TileGeometry and Tile:
1. Add to imports:
   - from .tile import TileGeometry, Tile

2. Add to __all__ list

3. Verify no cycles in imports
  </action>
  <verify>python -c "from projects.tile-platform.lib.foundation import TileGeometry, Tile"</verify>
  <done>TileGeometry and Tile are exported from foundation package</done>
</task>

</tasks>

<verification>
1. TileGeometry generates correct vertices for each shape
2. Square tiles have 4 vertices, 1 face
3. Octagonal tiles have 8 vertices, 1 face
4. Hexagonal tiles have 6 vertices, 1 face
5. Platform.place_tile() successfully adds tiles
6. Platform.remove_tile() successfully removes tiles
7. All code is testable without Blender
</verification>

<success_criteria>
- Tiles have geometry that can be generated procedurally
- Tiles support multiple shapes (square, octagonal, hexagonal)
- Tile geometry is Blender-independent for testing
- Platform can place and remove tiles at grid positions
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation/01-02-SUMMARY.md`
</output>
