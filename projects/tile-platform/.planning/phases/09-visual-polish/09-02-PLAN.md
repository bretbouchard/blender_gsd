---
phase: 09-visual-polish
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - projects/tile-platform/lib/style/feedback.py
  - projects/tile-platform/lib/style/__init__.py
autonomous: true

must_haves:
  truths:
    - "Motion has satisfying weight and mechanical authenticity"
    - "Visual feedback emphasizes mechanical nature"
    - "System provides satisfying user experience"
    - "Polish is consistent across all features"
  artifacts:
    - path: "projects/tile-platform/lib/style/feedback.py"
      provides: "VisualFeedback and MotionPolish"
      min_lines: 50
  key_links:
    - from: "lib/style/feedback.py"
      to: "lib/arms/arm.py"
      via: "import"
      pattern: "from\\.\\.arms import Arm"
---

<objective>
Create motion polish system for satisfying mechanical feedback.

Purpose: Add final polish to arm and tile motion.
Output: Working VisualFeedback and MotionPolish classes.
</objective>

<tasks>

<task type="auto">
  <name>Create visual feedback system</name>
  <files>projects/tile-platform/lib/style/feedback.py</files>
  <action>
Create VisualFeedback for motion satisfaction:
1. define FeedbackType enum:
   - TILE_PLACE: Tile placement feedback
   - TILE_REMOVE: Tile removal feedback
   - ARM_LOCK: Arm locked in position
   - ARM_RELEASE: Arm released for movement

2. Define VisualFeedback class with:
   - feedback_type: FeedbackType
   - duration: float
   - intensity: float

3. Define MotionPolish class with:
   - overshoot_amount: float (default 0.0)
   - settle_time: float (default 0.2)
   - ease_curve: str (default "ease_out_elastic")

4. Implement methods:
   - apply_overshoot(target_pos: Tuple, float, float, float) -> Tuple[float, float, float]
     * Adds overshoot to motion
     * Returns position with overshoot

   - apply_settle(current_pos: Tuple, float, float, float, target_pos: Tuple[float, float, float]) -> Tuple[float, float, float]
     * Applies settling motion
     * Returns settled position

   - create_feedback(event: FeedbackType) -> VisualFeedback
     * Creates feedback for event type
  </action>
  <verify>python -c projects/tile-platform/lib/style/feedback.py</verify>
  <done>VisualFeedback adds satisfying motion polish</done>
</task>

<task type="auto">
  <name>Update package exports</name>
  <files>projects/tile-platform/lib/style/__init__.py</files>
  <action>
Update package exports:
1. Add to imports:
   - from .feedback import VisualFeedback, MotionPolish, FeedbackType

2. Add to __all__ list
  </action>
  <verify>python -c "from projects.tile-platform.lib.style import VisualFeedback, MotionPolish"</verify>
  <done>VisualFeedback and MotionPolish are exported</done>
</task>

</tasks>

<verification>
1. Motion has satisfying weight and mechanical authenticity
2. Visual feedback emphasizes mechanical nature
3. Overshoot and settle feel natural
4. Feedback is consistent across all features
5. Polish enhances user experience
</verification>

<success_criteria>
- Motion has satisfying weight and mechanical authenticity
- Visual feedback emphasizes mechanical nature
- System provides satisfying user experience
- Polish is consistent across all features
</success_criteria>

<output>
After completion, create `.planning/phases/09-visual-polish/09-02-SUMMARY.md`
</output>
