---
phase: 14-hybrid-deprecation
plan: 01
type: execute
wave: 1
depends_on: ["13-01"]
files_modified:
  - projects/tile-platform/lib/tile_platform_hybrid/__init__.py
  - projects/tile-platform/lib/tile_platform_hybrid/dispatcher.py
  - projects/tile-platform/lib/tile_platform_hybrid/compat.py
  - projects/tile-platform/docs/GN_MIGRATION_GUIDE.md
autonomous: true

must_haves:
  truths:
    - "Hybrid mode allows Python OR GN selection at runtime"
    - "Migration guide documents the GN API"
    - "Deprecation warnings guide users to GN"
    - "Python reference is archived but accessible"
  artifacts:
    - path: "projects/tile-platform/lib/tile_platform_hybrid/dispatcher.py"
      provides: "Runtime mode selection"
      min_lines: 80
    - path: "projects/tile-platform/docs/GN_MIGRATION_GUIDE.md"
      provides: "Complete migration documentation"
      min_lines: 200
---

<objective>
Create hybrid mode and complete migration documentation.

Purpose: Provide smooth transition path from Python to GN.
Output: Production-ready hybrid system with full documentation.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Create hybrid mode dispatcher</name>
  <files>projects/tile-platform/lib/tile_platform_hybrid/dispatcher.py</files>
  <action>
Create the hybrid mode dispatcher:
1. Define TilePlatformDispatcher class:
   - mode: str (python, gn, hybrid)
   - python_platform: Platform instance
   - gn_platform: PlatformGN instance

2. Implement mode selection:
   - Environment variable: TILE_PLATFORM_MODE
   - Values: "python", "gn", "hybrid"
   - Default: "gn" (after Phase 14)

3. Implement dispatch methods:
   - get_platform() -> Platform | PlatformGN
   - get_tile_system() -> TileSystem | TileGN
   - get_arm_system() -> Arm | ArmGN

4. Implement hybrid mode:
   - Use GN for rendering
   - Use Python for validation
   - Compare outputs in real-time
   - Log differences

5. Add deprecation warnings:
   - warn_if_python_mode() -> warning message
   - Suggest migration to GN
  </action>
  <verify>TILE_PLATFORM_MODE=python python -c "from projects.tile-platform.lib.tile_platform_hybrid import TilePlatformDispatcher; print('OK')"</verify>
  <done>Hybrid dispatcher allows runtime mode selection</done>
</task>

<task type="auto">
  <name>Create compatibility layer</name>
  <files>projects/tile-platform/lib/tile_platform_hybrid/compat.py</files>
  <action>
Create backward compatibility layer:
1. Define CompatLayer class:
   - Maps old Python API to new GN API
   - Provides deprecation warnings

2. Implement API adapters:
   - PlatformCompat -> wraps PlatformGN with Platform API
   - TileCompat -> wraps TileGN with Tile API
   - ArmCompat -> wraps ArmGN with Arm API

3. Add migration helpers:
   - convert_config(old_config) -> new_config
   - validate_migration() -> list of issues

4. Document breaking changes:
   - API differences
   - Behavior changes
   - Required updates
  </action>
  <verify>python -c "from projects.tile-platform.lib.tile_platform_hybrid import CompatLayer; print('OK')"</verify>
  <done>Compatibility layer provides smooth migration</done>
</task>

<task type="auto">
  <name>Create GN Migration Guide</name>
  <files>projects/tile-platform/docs/GN_MIGRATION_GUIDE.md</files>
  <action>
Create comprehensive migration documentation:
1. Document GN Architecture:
   - NodeTreeBuilder usage
   - SimulationBuilder for physics
   - InstanceController for tiles
   - Named attributes for shaders

2. Document API mapping:
   - Python Platform -> GN PlatformGN
   - Python Tile -> GN TileGN
   - Python Arm -> GN ArmGN
   - Side-by-side code examples

3. Document migration steps:
   - Step 1: Update imports
   - Step 2: Replace class instantiation
   - Step 3: Update configuration
   - Step 4: Test with hybrid mode
   - Step 5: Switch to GN-only mode

4. Document troubleshooting:
   - Common issues
   - Performance differences
   - Visual differences

5. Include code examples:
   - Before/after snippets
   - Best practices
   - Performance tips
  </action>
  <verify>test -f projects/tile-platform/docs/GN_MIGRATION_GUIDE.md && wc -l projects/tile-platform/docs/GN_MIGRATION_GUIDE.md</verify>
  <done>Migration guide documents complete GN API</done>
</task>

<task type="auto">
  <name>Update STATE.md with migration status</name>
  <files>projects/tile-platform/.planning/STATE.md</files>
  <action>
Update STATE.md to reflect migration completion:
1. Update current position:
   - Phase: 14 of 14 (Hybrid & Deprecation)
   - Status: COMPLETE
   - Progress: 100%

2. Add migration metrics:
   - Total phases: 14 (9 Python + 5 GN migration)
   - GN coverage: 100%
   - Parity validation: PASSED

3. Add deprecation notice:
   - Python implementation archived
   - Available at tag v1.0-python-stable
   - Use GN implementation for new work

4. Add decisions log:
   - Decision: GN over Python for production
   - Reason: Performance, integration, maintainability
  </action>
  <verify>grep -q "Phase 14" projects/tile-platform/.planning/STATE.md</verify>
  <done>STATE.md reflects migration completion</done>
</task>

</tasks>

<verification>
1. Hybrid mode allows Python/GN selection
2. Compatibility layer provides backward compatibility
3. Migration guide is comprehensive (200+ lines)
4. STATE.md updated with migration status
5. All documentation complete
</verification>

<success_criteria>
- Hybrid mode allows Python OR GN selection at runtime
- Migration guide documents the GN API
- Deprecation warnings guide users to GN
- Python reference is archived but accessible
- Complete migration from Python to GN achieved
</success_criteria>

<output>
After completion, create `.planning/phases/14-hybrid-deprecation/14-01-SUMMARY.md`
</output>
