---
phase: 07-automated-following
plan: 01
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - projects/tile-platform/lib/following/__init__.py
  - projects/tile-platform/lib/following/tracker.py
  - projects/tile-platform/lib/following/controller.py
autonomous: true

must_haves:
  truths:
    - "Platform automatically follows a target path"
    - "System predicts tile placement ahead of time"
    - "Following is smooth and responsive"
    - "Platform maintains optimal distance from target"
  artifacts:
    - path: "projects/tile-platform/lib/following/tracker.py"
      provides: "TargetTracker"
      min_lines: 50
    - path: "projects/tile-platform/lib/following/controller.py"
      provides: "FollowingController"
      min_lines: 60
  key_links:
    - from: "lib/following/controller.py"
      to: "lib/following/tracker.py"
      via: "composition"
      pattern: "self\\.tracker\\s*=\\s*TargetTracker"
---

<objective>
Create automated following system for target tracking.

Purpose: Enable platform to automatically follow paths, characters, or objects.
Output: Working following system with prediction.
</objective>

<tasks>

<task type="auto">
  <name>Create TargetTracker</name>
  <files>projects/tile-platform/lib/following/tracker.py</files>
  <action>
Create TargetTracker for path/character tracking:
1. Define TargetTracker class with:
   - target_path: List[Tuple[float, float, float]]
   - current_index: int
   - lookahead_distance: float (default 5.0)

2. Implement methods:
   - update_target(new_path: List[Tuple[float, float, float]]) -> None
     * Updates target path
     * Resets current_index

   - get_current_target() -> Tuple[float, float, float]
     * Returns current target position

   - get_future_targets(count: int) -> List[Tuple[float, float, float]]
     * Returns next N target positions
     * Uses lookahead_distance

   - predict_position(steps_ahead: int) -> Tuple[float, float, float]
     * Predicts future target position
     * Uses velocity and acceleration
  </action>
  <verify>python -c projects/tile-platform/lib/following/tracker.py</verify>
  <done>TargetTracker tracks and predicts target positions</done>
</task>

<task type="auto">
  <name>Create FollowingController</name>
  <files>projects/tile-platform/lib/following/controller.py</files>
  <action>
Create FollowingController for automated following:
1. Define FollowingController class with:
   - platform: Platform
   - tracker: TargetTracker
   - tile_placer: TilePlacer
   - tile_retractor: TileRetractor

2. Implement methods:
   - update() -> None
     * Updates platform position based on target
     * Places tiles ahead of target
     * Removes tiles behind target

   - set_follow_distance(distance: float) -> None
     * Configures distance platform maintains from target

   - get_required_tiles() -> List[Tuple[int, int]]
     * Calculates which tile positions are needed
     * Based on target path and lookahead

3. Integration with tile system:
   - Use TilePlacer for placing tiles
   - Use TileRetractor for removing tiles
   - Maintain smooth platform movement
  </action>
  <verify>python -c projects/tile-platform/lib/following/controller.py</verify>
  <done>FollowingController automates platform following</done>
</task>

<task type="auto">
  <name>Create package exports</name>
  <files>projects/tile-platform/lib/following/__init__.py</files>
  <action>
Create package exports:
1. Export all public classes:
   - from .tracker import TargetTracker
   - from .controller import FollowingController

2. Define __all__ list
  </action>
  <verify>python -c "from projects.tile-platform.lib.following import TargetTracker, FollowingController"</verify>
  <done>All following classes are exported</done>
</task>

</tasks>

<verification>
1. TargetTracker accurately tracks target position
2. FollowingController places tiles ahead of target
3. FollowingController removes tiles behind target
4. Platform movement is smooth and responsive
5. System maintains optimal distance from target
</verification>

<success_criteria>
- Platform automatically follows a target path
- System predicts tile placement ahead of time
- Following is smooth and responsive
- Platform maintains optimal distance from target
</success_criteria>

<output>
After completion, create `.planning/phases/07-automated-following/07-01-SUMMARY.md`
</output>
