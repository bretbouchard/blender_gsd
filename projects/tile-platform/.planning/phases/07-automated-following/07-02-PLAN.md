---
phase: 07-automated-following
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - projects/tile-platform/lib/following/predictor.py
  - projects/tile-platform/lib/following/__init__.py
autonomous: true

must_haves:
  truths:
    - "System predicts tile placement needs ahead of time"
    - "Tile placement is smooth without visual stuttering"
    - "Predictive placement minimizes wait time"
    - "Removal is properly timed"
  artifacts:
    - path: "projects/tile-platform/lib/following/predictor.py"
      provides: "PlacementPredictor"
      min_lines: 40
  key_links:
    - from: "lib/following/predictor.py"
      to: "lib/following/tracker.py"
      via: "import"
      pattern: "from\\.tracker import TargetTracker"
---

<objective>
Create predictive placement system for smooth following.

Purpose: Predict tile placement needs to avoid visual stuttering.
Output: Working PlacementPredictor class.
</objective>

<tasks>

<task type="auto">
  <name>Create PlacementPredictor</name>
  <files>projects/tile-platform/lib/following/predictor.py</files>
  <action>
Create PlacementPredictor for predictive tile placement:
1. Define PlacementPredictor class with:
   - tracker: TargetTracker
   - prediction_horizon: int (default 10)
   - confidence_threshold: float (default 0.0)

2. Implement methods:
   - predict_placements(current_pos: Tuple[int, int]) -> List[Tuple[int, int]]
     * Predicts which tiles will be needed
     * Returns list of predicted tile positions

   - predict_removals(current_pos: Tuple[int, int]) -> List[Tuple[int, int]]
     * Predicts which tiles can be removed
     * Returns list of tile positions to remove

   - update_with_actual(actual_placements: List[Tuple[int, int]]) -> None
     * Updates prediction model with actual data
     * Improves future predictions

3. Prediction strategies:
   - Use target velocity to predict future positions
   - Maintain buffer zone around target
   - Account for target acceleration
  </action>
  <verify>python -c projects/tile-platform/lib/following/predictor.py</verify>
  <done>PlacementPredictor predicts tile placement needs</done>
</task>

<task type="auto">
  <name>Update package exports</name>
  <files>projects/tile-platform/lib/following/__init__.py</files>
  <action>
Update package exports:
1. Add to imports:
   - from .predictor import PlacementPredictor

2. Add to __all__ list
  </action>
  <verify>python -c "from projects.tile-platform.lib.following import PlacementPredictor"</verify>
  <done>PlacementPredictor is exported from following package</done>
</task>

</tasks>

<verification>
1. PlacementPredictor accurately predicts tile needs
2. Predictive placement reduces wait time
3. Removal timing is correct
4. Following is smooth without visual stuttering
5. Predictions improve over time with feedback
</verification>

<success_criteria>
- System predicts tile placement needs ahead of time
- Tile placement is smooth without visual stuttering
- Predictive placement minimizes wait time
- Removal is properly timed
</success_criteria>

<output>
After completion, create `.planning/phases/07-automated-following/07-02-SUMMARY.md`
</output>
