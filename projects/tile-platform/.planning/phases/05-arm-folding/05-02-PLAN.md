---
phase: 05-arm-folding
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - projects/tile-platform/lib/folding/controller.py
  - projects/tile-platform/lib/folding/__init__.py
autonomous: true

must_haves:
  truths:
    - "FoldingController integrates with tile placement system"
    - "Arms fold automatically when tile is removed"
    - "Arms deploy automatically when tile is placed"
    - "System handles concurrent arm operations"
  artifacts:
    - path: "projects/tile-platform/lib/folding/controller.py"
      provides: "FoldingController"
      min_lines: 50
  key_links:
    - from: "lib/folding/controller.py"
      to: "lib/folding/animator.py"
      via: "composition"
      pattern: "self\\.animator\\s*=\\s*FoldingAnimator"
---

<objective>
Create folding controller that integrates with tile system.

Purpose: Automate arm folding based on tile placement state.
Output: Working FoldingController class.
</objective>

<tasks>

<task type="auto">
  <name>Create FoldingController</name>
  <files>projects/tile-platform/lib/folding/controller.py</files>
  <action>
Create FoldingController for automated folding:
1. Define FoldingController class with:
   - animator: FoldingAnimator
   - tile_registry: TileRegistry
   - arm_status: Dict[int, str] (arm_id -> "idle" | "busy" | "folding" | "deploying")

2. Implement methods:
   - on_tile_placed(tile_pos: Tuple[int, int], arm_index: int) -> None
     * Called when tile is placed
     * Deploys arm if folded
     * Updates arm_status

   - on_tile_removed(tile_pos: Tuple[int, int], arm_index: int) -> None
     * Called when tile is removed
     * Folds arm if idle
     * Updates arm_status

   - update(dt: float) -> Dict[int, FoldingPose]
     * Advances animations
     * Returns current pose states
     * Transitions between states

   - get_arm_for_tile(tile_pos: Tuple[int, int]) -> Optional[int]
     * Finds arm responsible for tile position
     * Returns None if no arm assigned

   - assign_arm_to_position(arm_index: int, tile_pos: Tuple[int, int]) -> None
     * Assigns arm to tile position
     * Updates internal mappings

3. State transitions:
   - idle -> deploying -> busy -> folding -> idle
   - Invalid transitions raise error
  </action>
  <verify>python -c projects/tile-platform/lib/folding/controller.py</verify>
  <done>FoldingController automates arm folding based on tile state</done>
</task>

<task type="auto">
  <name>Update package exports</name>
  <files>projects/tile-platform/lib/folding/__init__.py</files>
  <action>
Update package exports:
1. Add to imports:
   - from .controller import FoldingController

2. Add to __all__ list
  </action>
  <verify>python -c "from projects.tile-platform.lib.folding import FoldingController"</verify>
  <done>FoldingController is exported from folding package</done>
</task>

</tasks>

<verification>
1. FoldingController integrates with tile placement system
2. Arms fold automatically when tile is removed
3. Arms deploy automatically when tile is placed
4. System handles concurrent arm operations
5. State transitions follow valid sequence
</verification>

<success_criteria>
- FoldingController integrates with tile placement system
- Arms fold automatically when tile is removed
- Arms deploy automatically when tile is placed
- System handles concurrent arm operations
</success_criteria>

<output>
After completion, create `.planning/phases/05-arm-folding/05-02-SUMMARY.md`
</output>
