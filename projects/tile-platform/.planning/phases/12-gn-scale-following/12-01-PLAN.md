---
phase: 12-gn-scale-following
plan: 01
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - projects/tile-platform/lib/gn_platform/scale_nodes.py
  - projects/tile-platform/lib/gn_platform/following_nodes.py
  - projects/tile-platform/lib/gn_platform/__init__.py
autonomous: true

must_haves:
  truths:
    - "Procedural generation uses GN simulation zones"
    - "Path following is implemented in GN"
    - "Character tracking uses object info nodes"
    - "System handles unlimited scale efficiently"
  artifacts:
    - path: "projects/tile-platform/lib/gn_platform/scale_nodes.py"
      provides: "Unlimited scale via procedural generation"
      min_lines: 100
    - path: "projects/tile-platform/lib/gn_platform/following_nodes.py"
      provides: "Path/character following in GN"
      min_lines: 120
---

<objective>
Migrate scale and following systems to Geometry Nodes.

Purpose: Enable unlimited scale and automated following via GN simulation.
Output: Working GN-based scale and following systems.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/geometry_nodes/simulation.py
@lib/scale/manager.py
@lib/following/tracker.py
</context>

<tasks>

<task type="auto">
  <name>Create ScaleGN for procedural generation</name>
  <files>projects/tile-platform/lib/gn_platform/scale_nodes.py</files>
  <action>
Create the ScaleGN class for unlimited scale:
1. Define ScaleGN class:
   - builder: NodeTreeBuilder reference
   - sim: SimulationBuilder instance
   - chunk_size: float (default 10.0)

2. Implement infinite grid simulation:
   - create_scale_zone() method
   - State: active_chunks (GEOMETRY)
   - Track visible chunks based on camera

3. Implement chunk generation:
   - generate_chunks(camera_pos, view_distance)
   - Calculate required chunk coordinates
   - Instance tiles within each chunk
   - Use seeded random for determinism

4. Implement LOD system:
   - Distance-based tile detail
   - Near: Full detail tiles
   - Mid: Simplified tiles
   - Far: Point representation only

5. Reference Python implementation at `lib/scale/manager.py`
  </action>
  <verify>python -c "from projects.tile-platform.lib.gn_platform import ScaleGN; print('OK')"</verify>
  <done>ScaleGN creates procedural infinite grid</done>
</task>

<task type="auto">
  <name>Create FollowingGN for path/character tracking</name>
  <files>projects/tile-platform/lib/gn_platform/following_nodes.py</files>
  <action>
Create the FollowingGN class for automated following:
1. Define FollowingGN class:
   - builder: NodeTreeBuilder reference
   - sim: SimulationBuilder instance

2. Implement path tracking simulation:
   - create_following_zone() method
   - State variables:
     * current_path_index: INT
     * platform_position: VECTOR
     * target_position: VECTOR

3. Implement path following logic:
   - follow_path(path_points, speed)
   - Advance along path points
   - Interpolate between waypoints
   - Update platform position

4. Implement character tracking:
   - track_object(object_info)
   - Get object position from scene
   - Calculate platform offset
   - Smooth follow with damping

5. Implement tile placement triggers:
   - Position-based tile activation
   - Lookahead for tile placement
   - Keep-behind for tile removal

6. Reference Python implementation at `lib/following/tracker.py`
  </action>
  <verify>python -c "from projects.tile-platform.lib.gn_platform import FollowingGN; print('OK')"</verify>
  <done>FollowingGN implements path and character tracking</done>
</task>

<task type="auto">
  <name>Update package exports</name>
  <files>projects/tile-platform/lib/gn_platform/__init__.py</files>
  <action>
Update __init__.py to include new classes:
1. Add imports:
   - from .scale_nodes import ScaleGN
   - from .following_nodes import FollowingGN

2. Update __all__ list
  </action>
  <verify>python -c "from projects.tile-platform.lib.gn_platform import ScaleGN, FollowingGN"</verify>
  <done>ScaleGN and FollowingGN exported from package</done>
</task>

</tasks>

<verification>
1. ScaleGN generates chunks procedurally
2. LOD system reduces detail at distance
3. FollowingGN tracks paths correctly
4. Character tracking uses object info nodes
5. Tile placement triggers work correctly
</verification>

<success_criteria>
- Procedural generation uses GN simulation zones
- Path following is implemented in GN
- Character tracking uses object info nodes
- System handles unlimited scale efficiently
- Performance maintained at 200+ tiles
</success_criteria>

<output>
After completion, create `.planning/phases/12-gn-scale-following/12-01-SUMMARY.md`
</output>
