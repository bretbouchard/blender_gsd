---
phase: 02-tile-system
plan: 01
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - projects/tile-platform/lib/tiles/__init__.py
  - projects/tile-platform/lib/tiles/placer.py
  - projects/tile-platform/lib/tiles/retractor.py
  - projects/tile-platform/lib/tiles/registry.py
autonomous: true

must_haves:
  truths:
    - "TilePlacer can determine which tiles need to be placed"
    - "TileRetractor can determine which tiles can be removed"
    - "System maintains a path or target to follow"
    - "Tile placement is bidirectional"
  artifacts:
    - path: "projects/tile-platform/lib/tiles/placer.py"
      provides: "TilePlacer class"
      min_lines: 50
    - path: "projects/tile-platform/lib/tiles/retractor.py"
      provides: "TileRetractor class"
      min_lines: 50
    - path: "projects/tile-platform/lib/tiles/registry.py"
      provides: "TileRegistry for state management"
      min_lines: 40
  key_links:
    - from: "lib/tiles/placer.py"
      to: "lib/foundation/platform.py"
      via: "import"
      pattern: "from\\.\\.foundation import Platform"
---

<objective>
Create tile placement and retraction logic for path following.

Purpose: Enable intelligent tile placement along paths and retraction when no longer needed.
Output: Working tile placer and retractor classes.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Create TileRegistry for state management</name>
  <files>projects/tile-platform/lib/tiles/registry.py</files>
  <action>
Create the TileRegistry class for tracking tile state:
1. Define TileState enum:
   - PENDING: Tile needs to be placed
   - PLACED: Tile is currently placed
   - REMOVING: Tile is scheduled for removal
   - REMOVED: Tile has been removed

2. Define TileRegistry class with:
   - tile_states: Dict[Tuple[int, int], TileState]
   - target_path: Optional[List[Tuple[int, int]]]
   - placed_tiles: Set[Tuple[int, int]]

3. Implement methods:
   - set_target_path(path: List[Tuple[int, int]]) -> None
   - get_placement_candidates() -> List[Tuple[int, int]]
     * Returns positions along target path not yet placed
   - get_removal_candidates() -> List[Tuple[int, int]]
     * Returns placed positions not on target path
   - mark_placed(pos: Tuple[int, int]) -> None
   - mark_removing(pos: Tuple[int, int]) -> None
   - mark_removed(pos: Tuple[int, int]) -> None
  </action>
  <verify>python -c projects/tile-platform/lib/tiles/registry.py</verify>
  <done>TileRegistry tracks tile states and target path</done>
</task>

<task type="auto">
  <name>Create TilePlacer for placement logic</name>
  <files>projects/tile-platform/lib/tiles/placer.py</files>
  <action>
Create the TilePlacer class for determining placement:
1. Define TilePlacer class with:
   - platform: Platform instance
   - registry: TileRegistry instance
   - lookahead_distance: int (default 5)

2. Implement methods:
   - update_placements(current_pos: Tuple[int, int]) -> List[Tuple[int, int]]
     * Returns positions that should have tiles placed
     * Uses lookahead to place tiles ahead of current position
     * Updates registry with PENDING -> PLACED transitions

   - can_place_at(pos: Tuple[int, int]) -> bool
     * Checks if position is valid for placement
     * Considers grid bounds and existing tiles

   - get_next_placements(n: int) -> List[Tuple[int, int]]
     * Returns next N positions that need placement
     * Prioritizes positions closest to target path

3. Integration with Platform:
   - Call platform.place_tile() for each placement
   - Handle placement failures gracefully
  </action>
  <verify>python -c projects/tile-platform/lib/tiles/placer.py</verify>
  <done>TilePlacer determines and executes tile placement</done>
</task>

<task type="auto">
  <name>Create TileRetractor for removal logic</name>
  <files>projects/tile-platform/lib/tiles/retractor.py</files>
  <action>
Create the TileRetractor class for determining removal:
1. Define TileRetractor class with:
   - platform: Platform instance
   - registry: TileRegistry instance
   - keep_distance: int (default 3)
     * How far behind current position to keep tiles

2. Implement methods:
   - update_removals(current_pos: Tuple[int, int]) -> List[Tuple[int, int]]
     * Returns positions that should have tiles removed
     * Keeps tiles within keep_distance of current position
     * Updates registry with PLACED -> REMOVING -> REMOVED transitions

   - can_remove_at(pos: Tuple[int, int]) -> bool
     * Checks if position is safe to remove
     * Considers keep_distance and arm availability

   - get_next_removals(n: int) -> List[Tuple[int, int]]
     * Returns next N positions scheduled for removal
     * Prioritizes positions furthest from current position

3. Integration with Platform:
   - Call platform.remove_tile() for each removal
   - Store removed tile data for arm retrieval animation
  </action>
  <verify>python -c projects/tile-platform/lib/tiles/retractor.py</verify>
  <done>TileRetractor determines and executes tile removal</done>
</task>

<task type="auto">
  <name>Create package exports</name>
  <files>projects/tile-platform/lib/tiles/__init__.py</files>
  <action>
Create package exports for tiles module:
1. Export all public classes and enums:
   - from .registry import TileRegistry, TileState
   - from .placer import TilePlacer
   - from .retractor import TileRetractor

2. Define __all__ list

3. Add module docstring
  </action>
  <verify>python -c "from projects.tile-platform.lib.tiles import TileRegistry, TileState, TilePlacer, TileRetractor"</verify>
  <done>All tile classes are exported from package</done>
</task>

</tasks>

<verification>
1. TileRegistry correctly tracks tile states
2. TilePlacer identifies positions needing tiles
3. TileRetractor identifies tiles safe to remove
4. Placement and removal respect keep_distance and lookahead_distance
5. System is testable without Blender
</verification>

<success_criteria>
- TilePlacer can determine which tiles need to be placed
- TileRetractor can determine which tiles can be removed
- System maintains a path or target to follow
- Tile placement is bidirectional (can extend and retract)
</success_criteria>

<output>
After completion, create `.planning/phases/02-tile-system/02-01-SUMMARY.md`
</output>
