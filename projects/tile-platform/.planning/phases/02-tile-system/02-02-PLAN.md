---
phase: 02-tile-system
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - projects/tile-platform/lib/tiles/magneto.py
  - projects/tile-platform/lib/tiles/feedback.py
  - projects/tile-platform/lib/tiles/__init__.py
autonomous: true

must_haves:
  truths:
    - "Tiles provide visual feedback when connecting"
    - "Magneto-mechanical connection effect is visible"
    - "Feedback includes visual and animation cues"
    - "System provides satisfying tile attachment experience"
  artifacts:
    - path: "projects/tile-platform/lib/tiles/magneto.py"
      provides: "Magneto-mechanical connection system"
      min_lines: 60
    - path: "projects/tile-platform/lib/tiles/feedback.py"
      provides: "TileFeedback and VisualEffect classes"
      min_lines: 40
  key_links:
    - from: "lib/tiles/magneto.py"
      to: "lib/tiles/feedback.py"
      via: "composition"
      pattern: "self\\.feedback\\s*=\\s*TileFeedback"
---

<objective>
Implement magneto-mechanical tile connection system with visual feedback.

Purpose: Create satisfying visual feedback when tiles connect to the platform.
Output: Working magneto-mechanical connection system.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Create VisualEffect system</name>
  <files>projects/tile-platform/lib/tiles/feedback.py</files>
  <action>
Create the visual feedback system for tile connections:
1. Define VisualEffect enum with:
   - NONE - no effect
   - GLOW - emission glow
   - SPARK - electrical sparks
   - MAGNETIC - magnetic field lines
   - LOCK - mechanical lock indicator

2. Define TileFeedback dataclass with:
   - effect_type: VisualEffect
   - duration: float (seconds)
   - intensity: float (0.0 to 1.0)
   - color: Optional[Tuple[float, float, float, float]] (RGBA)
   - trigger_time: Optional[float]

3. Define FeedbackSequence dataclass with:
   - effects: List[TileFeedback]
   - total_duration: float

4. Create builder pattern for sequences:
   - connection_sequence() -> FeedbackSequence
   - disconnection_sequence() -> FeedbackSequence
  </action>
  <verify>python -c projects/tile-platform/lib/tiles/feedback.py</verify>
  <done>VisualEffect, TileFeedback, FeedbackSequence classes exist</done>
</task>

<task type="auto">
  <name>Create MagnetoMechanical connection system</name>
  <files>projects/tile-platform/lib/tiles/magneto.py</files>
  <action>
Create the magneto-mechanical tile connection system:
1. Define ConnectionStyle enum with:
   - INDUSTRIAL - rough, mechanical look
   - HIGH_TECH - sleek, electronic look
   - BRUTALIST - bold, geometric look

2. Define MagnetoConfig dataclass with:
   - style: ConnectionStyle
   - feedback: FeedbackSequence
   - connection_strength: float (0.0 to 1.0)
   - magnetic_field_radius: float

3. Define MagnetoMechanical class with:
   - config: MagnetoConfig
   - connected_tiles: Set[Tuple[int, int]]

4. Implement methods:
   - connect_tile(pos: Tuple[int, int]) -> TileFeedback
     * Returns feedback to play when tile connects
   - disconnect_tile(pos: Tuple[int, int]) -> TileFeedback
     * Returns feedback to play when tile disconnects
   - is_connected(pos: Tuple[int, int]) -> bool
   - get_connection_strength(pos: Tuple[int, int]) -> float

5. Each connection should:
   - Play feedback sequence
   - Track connected state
   - Calculate strength based on distance to center
  </action>
  <verify>python -c projects/tile-platform/lib/tiles/magneto.py</verify>
  <done>MagnetoMechanical class handles tile connections with feedback</done>
</task>

<task type="auto">
  <name>Update package exports</name>
  <files>projects/tile-platform/lib/tiles/__init__.py</files>
  <action>
Update package exports to include magneto-mechanical system:
1. Add to imports:
   - from .feedback import VisualEffect, TileFeedback, FeedbackSequence
   - from .magneto import MagnetoConfig, MagnetoMechanical, ConnectionStyle

2. Add to __all__ list

3. Maintain existing exports
  </action>
  <verify>python -c "from projects.tile-platform.lib.tiles import VisualEffect, TileFeedback, MagnetoMechanical"</verify>
  <done>Magneto-mechanical classes are exported from tiles package</done>
</task>

</tasks>

<verification>
1. VisualEffect enum covers all effect types
2. TileFeedback provides configurable visual feedback
3. MagnetoMechanical tracks connections and provides feedback
4. Feedback sequences play on correct order
5. All code is testable without Blender
</verification>

<success_criteria>
- Tiles provide visual feedback when connecting
- Magneto-mechanical connection effect is visible
- Feedback includes visual and animation cues
- System provides satisfying tile attachment experience
</success_criteria>

<output>
After completion, create `.planning/phases/02-tile-system/02-02-SUMMARY.md`
</output>
