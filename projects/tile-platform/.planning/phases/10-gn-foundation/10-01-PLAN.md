---
phase: 10-gn-foundation
plan: 01
type: execute
wave: 1
depends_on: ["09-02"]
files_modified:
  - projects/tile-platform/lib/gn_platform/__init__.py
  - projects/tile-platform/lib/gn_platform/platform_nodes.py
  - projects/tile-platform/lib/gn_platform/tile_nodes.py
  - projects/tile-platform/lib/gn_platform/validation.py
autonomous: true

must_haves:
  truths:
    - "Platform geometry is generated via Geometry Nodes, not Python"
    - "NodeTreeBuilder is used for all node tree creation"
    - "InstanceController handles tile distribution"
    - "Named attributes store masks for shader access"
  artifacts:
    - path: "projects/tile-platform/lib/gn_platform/platform_nodes.py"
      provides: "PlatformGN class using NodeTreeBuilder"
      min_lines: 80
    - path: "projects/tile-platform/lib/gn_platform/tile_nodes.py"
      provides: "TileGN class with shape variants"
      min_lines: 100
    - path: "projects/tile-platform/lib/gn_platform/validation.py"
      provides: "Parity testing between Python and GN"
      min_lines: 60
  key_links:
    - from: "lib/gn_platform/platform_nodes.py"
      to: "lib/geometry_nodes/node_builder.py"
      via: "import"
      pattern: "from lib.geometry_nodes import NodeTreeBuilder"
    - from: "lib/gn_platform/tile_nodes.py"
      to: "lib/geometry_nodes/instances.py"
      via: "import"
      pattern: "from lib.geometry_nodes import InstanceController"
---

<objective>
Create the Geometry Nodes foundation for the tile platform system.

Purpose: Migrate from pure Python simulation to proper Blender Geometry Nodes architecture.
Output: Working GN-based platform and tile system with validation suite.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@projects/tile-platform/.planning/research/STACK.md
@lib/geometry_nodes/__init__.py
@lib/geometry_nodes/node_builder.py
</context>

<tasks>

<task type="auto">
  <name>Create GN platform package structure</name>
  <files>projects/tile-platform/lib/gn_platform/__init__.py</files>
  <action>
Create the GN platform package with proper exports:
1. Create `projects/tile-platform/lib/gn_platform/` directory
2. Define `__init__.py` with exports:
   - PlatformGN - Main platform geometry node builder
   - TileGN - Tile shape variants as GN node groups
   - GNValidator - Parity testing between Python and GN outputs
3. Import from parent `lib.geometry_nodes`:
   - NodeTreeBuilder
   - InstanceController
   - SimulationBuilder
   - FieldOperations
   - AttributeManager
4. Add module docstring explaining GN architecture
  </action>
  <verify>python -c "from projects.tile-platform.lib.gn_platform import PlatformGN, TileGN, GNValidator"</verify>
  <done>GN platform package created with all exports</done>
</task>

<task type="auto">
  <name>Create PlatformGN class with NodeTreeBuilder</name>
  <files>projects/tile-platform/lib/gn_platform/platform_nodes.py</files>
  <action>
Create the PlatformGN class using NodeTreeBuilder:
1. Define PlatformGN class:
   - builder: NodeTreeBuilder instance
   - tree_name: str (default "TilePlatform")
   - stages: dict mapping stage names to methods

2. Implement Universal Stage Order:
   - _stage_normalize(): Input parameter canonicalization
   - _stage_primary(): Base platform grid geometry
   - _stage_secondary(): Platform modifications
   - _stage_detail(): Surface effects (masked)
   - _stage_output(): Named attributes and cleanup

3. Implement build() method:
   - Create Group Input node with parameters:
     * Width (float, default 10.0)
     * Depth (float, default 10.0)
     * Tile_Size (float, default 1.0)
     * Seed (int, default 42)
   - Execute stages in order
   - Connect to Group Output

4. Add input socket definitions:
   - Geometry (mesh input)
   - All PlatformConfig parameters as sockets

5. Reference Python implementation at `lib/foundation/platform.py` for parity

CRITICAL: Must import and use NodeTreeBuilder from lib.geometry_nodes
  </action>
  <verify>python -c "from projects.tile-platform.lib.gn_platform import PlatformGN; p = PlatformGN(); tree = p.build()" </verify>
  <done>PlatformGN creates valid Geometry Node tree</done>
</task>

<task type="auto">
  <name>Create TileGN class with InstanceController</name>
  <files>projects/tile-platform/lib/gn_platform/tile_nodes.py</files>
  <action>
Create the TileGN class for tile shape generation and distribution:
1. Define TileGN class:
   - builder: NodeTreeBuilder reference
   - controller: InstanceController for distribution

2. Implement tile shape node groups:
   - _build_square_tile(): 4-vertex square mesh
   - _build_hexagon_tile(): 6-vertex hexagon mesh
   - _build_octagon_tile(): 8-vertex octagon mesh
   Each shape should:
   - Center at origin (0, 0, 0)
   - Use correct CCW winding
   - Include UV coordinates

3. Implement distribute_tiles() method:
   - Use InstanceController.distribute_points()
   - Apply seeded random for deterministic placement
   - Instance tiles on grid points

4. Implement shape selection:
   - Named attribute "tile_shape" for variant selection
   - Switch node for shape variants

5. Reference Python implementation at `lib/foundation/tile.py` for parity

CRITICAL: Must import and use InstanceController from lib.geometry_nodes
  </action>
  <verify>python -c "from projects.tile-platform.lib.gn_platform import TileGN; t = TileGN(None); shapes = t.build_all_shapes()" </verify>
  <done>TileGN creates all tile shape variants</done>
</task>

<task type="auto">
  <name>Create GN validation suite</name>
  <files>projects/tile-platform/lib/gn_platform/validation.py</files>
  <action>
Create the GNValidator class for parity testing:
1. Define GNValidator class:
   - python_platform: Platform instance (pure Python)
   - gn_platform: PlatformGN instance (Geometry Nodes)
   - tolerance: float (default 0.001)

2. Implement validation methods:
   - validate_platform_dimensions() -> bool
     * Compare platform bounds between Python and GN
   - validate_tile_positions() -> bool
     * Compare tile grid positions
   - validate_tile_geometry() -> bool
     * Compare vertex counts and face counts
   - validate_all() -> dict
     * Run all validations, return results

3. Implement comparison helpers:
   - _compare_vertices(py_verts, gn_verts) -> float
     * Return max distance between corresponding vertices
   - _compare_faces(py_faces, gn_faces) -> bool
     * Check face connectivity matches

4. Add reporting:
   - generate_report() -> str
     * Markdown report of validation results

5. Store validation results for CI integration
  </action>
  <verify>python -c "from projects.tile-platform.lib.gn_platform import GNValidator; v = GNValidator(); print(v.validate_all())"</verify>
  <done>GNValidator can compare Python and GN outputs</done>
</task>

</tasks>

<verification>
1. PlatformGN uses NodeTreeBuilder (not raw bpy)
2. TileGN uses InstanceController for distribution
3. All tile shapes generate correct vertex counts
4. Validation suite can compare Python vs GN
5. Named attributes are stored for shader access
6. Universal Stage Order is followed
</verification>

<success_criteria>
- Platform geometry is generated via Geometry Nodes, not Python
- NodeTreeBuilder is used for all node tree creation
- InstanceController handles tile distribution
- Named attributes store masks for shader access
- Parity testing validates Python vs GN outputs match
</success_criteria>

<output>
After completion, create `.planning/phases/10-gn-foundation/10-01-SUMMARY.md`
</output>
