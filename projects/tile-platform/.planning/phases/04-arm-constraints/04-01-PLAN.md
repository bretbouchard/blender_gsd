---
phase: 04-arm-constraints
plan: 01
type: execute
wave: 1
depends_on: ["03-02"]
files_modified:
  - projects/tile-platform/lib/constraints/__init__.py
  - projects/tile-platform/lib/constraints/target_reach.py
  - projects/tile-platform/lib/constraints/limiters.py
autonomous: true

must_haves:
  truths:
    - "Arms are constrained to always reach their target positions"
    - "Constraint system enforces realistic joint limits"
    - "Constraints prevent impossible arm configurations"
    - "Target reach is guaranteed within tolerance"
  artifacts:
    - path: "projects/tile-platform/lib/constraints/target_reach.py"
      provides: "TargetReachConstraint"
      min_lines: 50
    - path: "projects/tile-platform/lib/constraints/limiters.py"
      provides: "Joint limiters"
      min_lines: 40
  key_links:
    - from: "lib/constraints/target_reach.py"
      to: "lib/arms/physics.py"
      via: "import"
      pattern: "from\\.\\.arms\\.physics import"
---

<objective>
Implement arm constraint system that guarantees target reach.

Purpose: Ensure arms always reach their targets despite physics simulation.
Output: Working constraint system with guaranteed target reach.
</objective>

<tasks>

<task type="auto">
  <name>Create target reach constraint</name>
  <files>projects/tile-platform/lib/constraints/target_reach.py</files>
  <action>
Create TargetReachConstraint class:
1. Define TargetReachConstraint class with:
   - max_deviation: float (default 00.1)
   - overshoot_damping: float (default 0.0)
   - settling_threshold: float (default 0.01)
   - correction_force: float (default 10.0)

2. Implement methods:
   - compute_correction(current_pos, target_pos, velocity, float dt) -> Tuple[float, float, float]
     * Returns correction force to apply
     * Accounts for overshoot and settling
     * Uses spring-damper model

   - is_reached(current_pos, target_pos, velocity) -> bool
     * Returns True if arm has settled at target
     * Checks position and velocity against thresholds

   - get_deviation(current_pos, target_pos) -> float
     * Returns distance from target
     * Used for monitoring and logging

3. Integration with PhysicsSimulator:
   - Apply correction force to each arm step
   - Track cumulative deviation
   - Log constraint violations
  </action>
  <verify>python -c projects/tile-platform/lib/constraints/target_reach.py</verify>
  <done>TargetReachConstraint computes correction forces and tracks settling</done>
</task>

<task type="auto">
  <name>Create joint angle limiters</name>
  <files>projects/tile-platform/lib/constraints/limiters.py</files>
  <action>
Create joint angle limiters:
1. Define JointLimiter class with:
   - joint_limits: Dict[int, Tuple[float, float]] (joint_id -> (min, max))
   - violation_force: float (default 50.0)
   - softness: float (default 0.0)

2. Implement methods:
   - clamp_angle(joint_id, current_angle, target_angle) -> float
     * Returns clamped angle within limits
     * Applies soft constraint at boundaries
     * Uses smooth interpolation

   - is_valid(joint_id, angle) -> bool
     * Returns True if angle is within limits

   - get_violation(joint_id, angle) -> float
     * Returns 0.0 if within limits, positive value for violation magnitude

3. Define JointLimitConfig dataclass:
   - joint_index: int
   - min_angle: float
   - max_angle: float
   - stiffness: float

4. Add presets:
   - HINGE_LIMITS: (-90, 90) degrees
   - TELESCOPE_LIMITS: (-180, 180) degrees
   - PRISMATIC_LIMITS: (-45, 45) degrees
  </action>
  <verify>python -c projects/tile-platform/lib/constraints/limiters.py</verify>
  <done>JointLimiter clamps angles to valid ranges with soft boundaries</done>
</task>

<task type="auto">
  <name>Create package exports</name>
  <files>projects/tile-platform/lib/constraints/__init__.py</files>
  <action>
Create package exports:
1. Export all public classes:
   - from .target_reach import TargetReachConstraint
   - from .limiters import JointLimiter, JointLimitConfig

2. Add presets:
   - HINGE_PRESET
   - TELESCOPE_PRESET
   - PRISMATIC_PRESET

3. Define __all__ list
  </action>
  <verify>python -c "from projects.tile-platform.lib.constraints import TargetReachConstraint, JointLimiter"</verify>
  <done>All constraint classes are exported</done>
</task>

</tasks>

<verification>
1. TargetReachConstraint computes correct correction forces
2. Correction forces reduce deviation over time
3. JointLimiter clamps angles to valid ranges
4. Limits are enforced with soft boundaries (no hard stops)
5. System guarantees target reach within tolerance
</verification>

<success_criteria>
- Arms are constrained to always reach their target positions
- Constraint system enforces realistic joint limits
- Constraints prevent impossible arm configurations
- Target reach is guaranteed within tolerance (default 0.1)
</success_criteria>

<output>
After completion, create `.planning/phases/04-arm-constraints/04-01-SUMMARY.md`
</output>
