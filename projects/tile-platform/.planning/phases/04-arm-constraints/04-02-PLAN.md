---
phase: 04-arm-constraints
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - projects/tile-platform/lib/constraints/solver.py
  - projects/tile-platform/lib/constraints/__init__.py
autonomous: true

must_haves:
  truths:
    - "Constraint solver integrates with physics simulation"
    - "Constraints are applied each physics step"
    - "Solver maintains arm stability"
    - "Combined system produces smooth, accurate movement"
  artifacts:
    - path: "projects/tile-platform/lib/constraints/solver.py"
      provides: "ConstraintSolver"
      min_lines: 60
  key_links:
    - from: "lib/constraints/solver.py"
      to: "lib/constraints/target_reach.py"
      via: "composition"
      pattern: "self\\.target_reach\\s*="
---

<objective>
Create integrated constraint solver that works with physics.

Purpose: Combine physics simulation with constraint enforcement in a unified system.
Output: Working ConstraintSolver class.
</objective>

<tasks>

<task type="auto">
  <name>Create ConstraintSolver</name>
  <files>projects/tile-platform/lib/constraints/solver.py</files>
  <action>
Create ConstraintSolver that integrates constraints with physics:
1. Define ConstraintSolver class with:
   - physics_simulator: PhysicsSimulator
   - target_reach: TargetReachConstraint
   - joint_limiter: JointLimiter
   - iteration_count: int (default 10)
   - convergence_threshold: float (default 0e-6)

2. Implement methods:
   - solve_step(dt: float) -> Dict[int, float]
     * Performs one physics step
     * Applies constraints
     * Returns joint corrections

   - solve_full(duration: float, steps: int) -> List[Dict[int, float]]
     * Runs full simulation
     * Applies constraints each step
     * Returns history of corrections

   - check_convergence() -> bool
     * Returns True if all arms have settled
     * Uses target_reach.is_reached() for each arm

   - apply_joint_limits(corrections: Dict[int, float]) -> Dict[int, float]
     * Applies joint limits to corrections
     * Returns modified corrections

3. Integration flow:
   - Run physics step
   - Calculate constraint corrections
   - Apply joint limits
   - Update arm positions
   - Check convergence
  </action>
  <verify>python -c projects/tile-platform/lib/constraints/solver.py</verify>
  <done>ConstraintSolver integrates physics with constraint enforcement</done>
</task>

<task type="auto">
  <name>Update package exports</name>
  <files>projects/tile-platform/lib/constraints/__init__.py</files>
  <action>
Update package exports to include ConstraintSolver:
1. Add to imports:
   - from .solver import ConstraintSolver

2. Add to __all__ list

3. Update package docstring
  </action>
  <verify>python -c "from projects.tile-platform.lib.constraints import ConstraintSolver"</verify>
  <done>ConstraintSolver is exported from constraints package</done>
</task>

</tasks>

<verification>
1. ConstraintSolver integrates with PhysicsSimulator
2. Constraints are applied each physics step
3. Solver maintains arm stability
4. Combined system produces smooth, accurate movement
5. Convergence is detected correctly
</verification>

<success_criteria>
- Constraint solver integrates with physics simulation
- Constraints are applied each physics step
- Solver maintains arm stability
- Combined system produces smooth, accurate movement
- All arms reach targets within iteration limit
</success_criteria>

<output>
After completion, create `.planning/phases/04-arm-constraints/04-02-SUMMARY.md`
</output>
