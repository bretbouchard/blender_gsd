---
phase: 13-gn-export-polish
plan: 01
type: execute
wave: 1
depends_on: ["12-01"]
files_modified:
  - projects/tile-platform/lib/gn_platform/export_nodes.py
  - projects/tile-platform/lib/gn_platform/material_nodes.py
  - projects/tile-platform/lib/gn_platform/__init__.py
  - projects/tile-platform/lib/gn_platform/validation.py
autonomous: true

must_haves:
  truths:
    - "GN output integrates with export pipeline"
    - "Materials use named attributes from GN"
    - "Performance is optimized for real-time"
    - "Python vs GN parity is validated"
  artifacts:
    - path: "projects/tile-platform/lib/gn_platform/export_nodes.py"
      provides: "GN to export pipeline integration"
      min_lines: 80
    - path: "projects/tile-platform/lib/gn_platform/material_nodes.py"
      provides: "Named attribute material integration"
      min_lines: 100
---

<objective>
Integrate GN pipeline with export and polish systems.

Purpose: Complete the GN migration with export integration and validation.
Output: Production-ready GN pipeline with full parity.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/export/fbx.py
@lib/export/gltf.py
@lib/style/materials.py
</context>

<tasks>

<task type="auto">
  <name>Create ExportGN for pipeline integration</name>
  <files>projects/tile-platform/lib/gn_platform/export_nodes.py</files>
  <action>
Create the ExportGN class for export integration:
1. Define ExportGN class:
   - builder: NodeTreeBuilder reference
   - format: str (fbx, gltf, usd)

2. Implement bake node integration:
   - add_bake_node() method
   - Configure bake settings
   - Set animation frame range

3. Implement export preparation:
   - prepare_for_export(geometry)
   - Apply final transforms
   - Merge duplicate vertices
   - Validate mesh integrity

4. Implement format-specific outputs:
   - for_fbx() -> baked geometry
   - for_gltf() -> baked geometry
   - for_usd() -> baked geometry

5. Integration with existing export module
  </action>
  <verify>python -c "from projects.tile-platform.lib.gn_platform import ExportGN; print('OK')"</verify>
  <done>ExportGN integrates with export pipeline</done>
</task>

<task type="auto">
  <name>Create MaterialGN for named attribute integration</name>
  <files>projects/tile-platform/lib/gn_platform/material_nodes.py</files>
  <action>
Create the MaterialGN class for shader integration:
1. Define MaterialGN class:
   - builder: NodeTreeBuilder reference

2. Implement named attribute storage:
   - store_height_mask(mask) -> named attribute
   - store_edge_mask(mask) -> named attribute
   - store_tile_type(type_id) -> named attribute
   - store_arm_state(state) -> named attribute

3. Implement material parameter passing:
   - define_material_inputs()
   - Color (RGBA)
   - Metalness (float)
   - Roughness (float)
   - Emission (RGB + strength)

4. Implement style presets:
   - brutalist_preset() -> MaterialConfig
   - high_tech_preset() -> MaterialConfig
   - industrial_preset() -> MaterialConfig

5. Reference Python implementation at `lib/style/materials.py`
  </action>
  <verify>python -c "from projects.tile-platform.lib.gn_platform import MaterialGN; print('OK')"</verify>
  <done>MaterialGN provides named attributes for shaders</done>
</task>

<task type="auto">
  <name>Implement performance optimization</name>
  <files>projects/tile-platform/lib/gn_platform/performance.py</files>
  <action>
Create performance optimization utilities:
1. Implement LOD system:
   - distance_based_lod(camera_pos, geometry)
   - 3 LOD levels: Full, Simplified, Point

2. Implement culling:
   - frustum_culling(camera_frustum, geometry)
   - distance_culling(max_distance, geometry)

3. Implement instancing optimization:
   - merge_similar_instances()
   - batch_instance_transforms()

4. Add performance profiling:
   - profile_node_tree(tree) -> dict
   - Identify bottleneck nodes
   - Suggest optimizations
  </action>
  <verify>python -c "from projects.tile-platform.lib.gn_platform import performance; print('OK')"</verify>
  <done>Performance optimization utilities available</done>
</task>

<task type="auto">
  <name>Final parity validation</name>
  <files>projects/tile-platform/lib/gn_platform/validation.py</files>
  <action>
Extend GNValidator for comprehensive parity testing:
1. Add comprehensive tests:
   - validate_arm_physics() -> detailed report
   - validate_scale_generation() -> detailed report
   - validate_following_behavior() -> detailed report
   - validate_export_output() -> detailed report

2. Implement benchmarking:
   - benchmark_python_vs_gn(iterations) -> timing comparison
   - memory_usage_comparison() -> memory report

3. Generate final report:
   - generate_parity_report() -> markdown
   - Include all test results
   - Document any differences

4. CI integration:
   - Output in JUnit format
   - Fail on parity mismatch > tolerance
  </action>
  <verify>python -c "from projects.tile-platform.lib.gn_platform import GNValidator; v = GNValidator(); print(v.generate_parity_report()[:100])"</verify>
  <done>Comprehensive parity validation available</done>
</task>

</tasks>

<verification>
1. ExportGN integrates with existing export pipeline
2. Named attributes are accessible in shaders
3. LOD and culling work correctly
4. Performance meets targets (60fps at 200+ tiles)
5. Parity tests pass with tolerance < 0.01
</verification>

<success_criteria>
- GN output integrates with export pipeline
- Materials use named attributes from GN
- Performance is optimized for real-time
- Python vs GN parity is validated (tolerance < 0.01)
- All 9 phases have GN equivalents
</success_criteria>

<output>
After completion, create `.planning/phases/13-gn-export-polish/13-01-SUMMARY.md`
</output>
