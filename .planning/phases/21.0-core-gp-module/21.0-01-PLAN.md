---
phase: 21.0-core-gp-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/grease_pencil/__init__.py
  - lib/grease_pencil/types.py
  - configs/grease_pencil/stroke_presets.yaml
autonomous: true

must_haves:
  truths:
    - "GPStrokeConfig dataclass exists with all required fields"
    - "GPMaterialConfig dataclass exists with GP-specific properties"
    - "GPLayerConfig dataclass exists with layer configuration"
    - "GPMaskConfig dataclass exists with mask support"
    - "All types support to_dict() and from_dict() serialization"
    - "YAML preset file loads stroke configurations"
  artifacts:
    - path: "lib/grease_pencil/types.py"
      provides: "GP configuration dataclasses"
      min_lines: 200
    - path: "lib/grease_pencil/__init__.py"
      provides: "Package exports"
      exports: ["GPStrokeConfig", "GPMaterialConfig", "GPLayerConfig", "GPMaskConfig"]
    - path: "configs/grease_pencil/stroke_presets.yaml"
      provides: "Stroke configuration presets"
  key_links:
    - from: "lib/grease_pencil/types.py"
      to: "lib/animation/types.py"
      via: "dataclass pattern"
      pattern: "@dataclass"
---

<objective>
Create GP configuration types with dataclasses following lib/animation/types.py pattern.

Purpose: Establish type-safe configuration system for Grease Pencil workflows
Output: GPStrokeConfig, GPMaterialConfig, GPLayerConfig, GPMaskConfig dataclasses
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21.0-core-gp-module/21.0-RESEARCH.md

# Existing type patterns to follow
@lib/animation/types.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GP Type Definitions</name>
  <files>lib/grease_pencil/types.py</files>
  <action>
Create lib/grease_pencil/types.py with the following dataclasses following the lib/animation/types.py pattern:

1. **Enums** (match RESEARCH.md examples):
   - StrokeStyle: SOLID, TEXTURE, GRADIENT
   - FillStyle: SOLID, GRADIENT, TEXTURE, PATTERN
   - StrokeMode: LINE, DOTS, BOX
   - DisplayMode: 3DSPACE, 2DIMAGE, 2DSCREEN
   - BlendMode: REGULAR, MULTIPLY, ADD, SUBTRACT, DIVIDE, etc.

2. **GPStrokeConfig** dataclass:
   - id: str
   - points: List[Tuple[float, float, float, float, float]] (x, y, z, pressure, strength)
   - stroke_width: float = 5.0
   - hardness: float = 1.0
   - use_cyclic: bool = False
   - material_index: int = 0
   - display_mode: DisplayMode = DisplayMode.D3SPACE
   - seed: Optional[int] = None
   - Include to_dict() and from_dict() methods

3. **GPMaterialConfig** dataclass:
   - id: str
   - stroke_style: StrokeStyle = StrokeStyle.SOLID
   - fill_style: FillStyle = FillStyle.SOLID
   - stroke_mode: StrokeMode = StrokeMode.LINE
   - color: Tuple[float, float, float, float] = (0.0, 0.0, 0.0, 1.0)
   - fill_color: Tuple[float, float, float, float] = (1.0, 1.0, 1.0, 1.0)
   - show_stroke: bool = True
   - show_fill: bool = True
   - use_stroke_holdout: bool = False
   - use_fill_holdout: bool = False
   - stroke_texture: Optional[str] = None
   - fill_texture: Optional[str] = None
   - gradient_type: str = "LINEAR"
   - mix_factor: float = 0.5
   - overlap_mode: str = "DEFAULT"
   - overlap_threshold: float = 0.1
   - Include to_dict() and from_dict() methods

4. **GPLayerConfig** dataclass:
   - id: str
   - name: str
   - opacity: float = 1.0
   - blend_mode: BlendMode = BlendMode.REGULAR
   - use_lights: bool = False
   - use_mask_layer: bool = False
   - stroke_configs: List[GPStrokeConfig] = field(default_factory=list)
   - Include to_dict() and from_dict() methods

5. **GPMaskConfig** dataclass:
   - enabled: bool = False
   - mask_layer: str = ""
   - invert: bool = False
   - feather: float = 0.0
   - type: str = "procedural"  # layer, stroke_weight, procedural, texture
   - blend_mode: str = "multiply"
   - Include to_dict() and from_dict() methods

6. **GPAnimationConfig** dataclass:
   - id: str
   - frame_start: int = 1
   - frame_end: int = 100
   - frame_rate: float = 24.0
   - onion_skin_enabled: bool = False
   - onion_skin_frames_before: int = 2
   - onion_skin_frames_after: int = 2
   - Include to_dict() and from_dict() methods

Use field(default_factory=list) for list fields to avoid mutable default issues.
Follow exact pattern from lib/animation/types.py for consistency.
  </action>
  <verify>
    - File exists at lib/grease_pencil/types.py
    - Contains all 5 enums
    - Contains all 6 dataclasses
    - All dataclasses have to_dict() and from_dict() methods
    - Python syntax is valid (can import without errors)
  </verify>
  <done>
GP type definitions complete with GPStrokeConfig, GPMaterialConfig, GPLayerConfig, GPMaskConfig, and GPAnimationConfig dataclasses following lib/animation/types.py pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create YAML Stroke Presets</name>
  <files>configs/grease_pencil/stroke_presets.yaml</files>
  <action>
Create configs/grease_pencil/stroke_presets.yaml with stroke configuration presets:

```yaml
# Stroke presets for common GP workflows

# Basic strokes
basic_line:
  id: basic_line
  stroke_width: 2.0
  hardness: 1.0
  display_mode: "3DSPACE"

thick_outline:
  id: thick_outline
  stroke_width: 5.0
  hardness: 1.0
  display_mode: "3DSPACE"

soft_sketch:
  id: soft_sketch
  stroke_width: 3.0
  hardness: 0.7
  display_mode: "3DSPACE"

# Artistic styles
anime_outline:
  id: anime_outline
  stroke_width: 3.0
  hardness: 1.0
  display_mode: "3DSPACE"

watercolor_stroke:
  id: watercolor_stroke
  stroke_width: 4.0
  hardness: 0.5
  display_mode: "3DSPACE"

comic_bold:
  id: comic_bold
  stroke_width: 6.0
  hardness: 1.0
  display_mode: "3DSPACE"

# Technical drawing
technical_line:
  id: technical_line
  stroke_width: 1.5
  hardness: 1.0
  display_mode: "3DSPACE"

blueprint_line:
  id: blueprint_line
  stroke_width: 2.0
  hardness: 1.0
  display_mode: "3DSPACE"

# Motion graphics
motion_shape:
  id: motion_shape
  stroke_width: 4.0
  hardness: 1.0
  display_mode: "3DSPACE"
  use_cyclic: true

text_outline:
  id: text_outline
  stroke_width: 2.5
  hardness: 1.0
  display_mode: "2DIMAGE"
```

Create the directory structure if it doesn't exist:
- configs/grease_pencil/
  </action>
  <verify>
    - Directory configs/grease_pencil/ exists
    - File stroke_presets.yaml exists
    - Contains at least 10 stroke presets
    - Valid YAML syntax
  </verify>
  <done>
YAML stroke presets file created with 10+ stroke configurations for common GP workflows.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Package Init File</name>
  <files>lib/grease_pencil/__init__.py</files>
  <action>
Create lib/grease_pencil/__init__.py with package exports:

```python
"""
Grease Pencil System - Node-centric 2D animation in Blender.

This module provides procedural GP generation following GSD's
Universal Stage Order with deterministic output.

Phase 21.0: Core GP Module (REQ-GP-01)
"""

from .types import (
    # Enums
    StrokeStyle,
    FillStyle,
    StrokeMode,
    DisplayMode,
    BlendMode,
    # Configs
    GPStrokeConfig,
    GPMaterialConfig,
    GPLayerConfig,
    GPMaskConfig,
    GPAnimationConfig,
)

__all__ = [
    # Enums
    'StrokeStyle',
    'FillStyle',
    'StrokeMode',
    'DisplayMode',
    'BlendMode',
    # Configs
    'GPStrokeConfig',
    'GPMaterialConfig',
    'GPLayerConfig',
    'GPMaskConfig',
    'GPAnimationConfig',
]

__version__ = '0.1.0'
```

Create the lib/grease_pencil/ directory if it doesn't exist.
  </action>
  <verify>
    - Directory lib/grease_pencil/ exists
    - File __init__.py exists
    - Can import types: from lib.grease_pencil import GPStrokeConfig
    - __all__ list matches imports
    - Version string present
  </verify>
  <done>
Package init file created with exports for all GP type definitions.
  </done>
</task>

</tasks>

<verification>
- All GP types follow lib/animation/types.py pattern
- YAML presets loadable
- Package importable from lib.grease_pencil
- Version 0.1.0 set
</verification>

<success_criteria>
- GPStrokeConfig, GPMaterialConfig, GPLayerConfig, GPMaskConfig, GPAnimationConfig dataclasses exist
- All dataclasses have to_dict() and from_dict() methods
- 10+ stroke presets in YAML
- Package exports all types
- Can import: from lib.grease_pencil import GPStrokeConfig, GPMaterialConfig, GPLayerConfig, GPMaskConfig
</success_criteria>

<output>
After completion, create `.planning/phases/21.0-core-gp-module/21.0-01-SUMMARY.md`
</output>
