---
phase: 07.5-advanced-features
plan: 05
type: execute
wave: 5
depends_on: ["07.5-01", "07.5-02", "07.5-03", "07.5-04"]
files_modified:
  - lib/cinematic/tracking/__init__.py
  - lib/cinematic/__init__.py
  - lib/__init__.py
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "All Phase 7.5 modules export from tracking/__init__.py"
    - "Version bumped to 0.4.0 marking MILESTONE v0.6 complete"
    - "Batch module exports from cinematic/__init__.py"
    - "All new modules have proper __all__ exports"
  artifacts:
    - path: "lib/cinematic/tracking/__init__.py"
      provides: "Complete tracking module exports"
    - path: "lib/cinematic/__init__.py"
      provides: "Cinematic package with batch module"
    - path: "pyproject.toml"
      provides: "Version 0.4.0"
  key_links:
    - from: "lib/cinematic/tracking/__init__.py"
      to: "All Phase 7.5 modules"
      via: "imports"
      pattern: "package_exports"
---

<objective>
Finalize Phase 7.5 with package exports, version bump to 0.4.0, marking MILESTONE v0.6 (Cinematic System) complete.

Purpose: Complete Phase 7.5 by updating all package exports and bumping version to 0.4.0.
Output: Updated __init__.py files, pyproject.toml with version 0.4.0.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Update tracking/__init__.py with all exports</name>
  <files>lib/cinematic/tracking/__init__.py</files>
  <action>
    Ensure `lib/cinematic/tracking/__init__.py` has complete exports for all Phase 7.5 additions:

    The file should have imports and __all__ entries for:
    - All existing types (Track, Solve, etc.)
    - All existing modules (camera_solver, point_tracker, etc.)
    - New Phase 7.5 additions:
      - Batch: BatchJob, BatchConfig, BatchResult, BatchProcessor
      - Object Tracker: ObjectTracker, PlanarTracker, KnobTracker, RigidBodyTracker, FaderTracker
      - Scan Import: ScanData, FloorPlane, ScaleCalibration, ScanImporter
      - Mocap: MocapData, HandAnimation, MocapImporter, MocapRetargeter, ButtonPressDetector

    Final __all__ should include all exports in alphabetical order for easy scanning.
  </action>
  <verify>python -c "from lib.cinematic.tracking import *; print('Tracking exports OK')"</verify>
  <done>tracking/__init__.py has complete exports for Phase 7.5</done>
</task>

<task type="auto">
  <name>Update cinematic/__init__.py with batch module</name>
  <files>lib/cinematic/__init__.py</files>
  <action>
    Read current `lib/cinematic/__init__.py` and add batch module exports:

    Add to imports:
    ```python
    from .batch import (
        BatchJob,
        BatchConfig,
        BatchResult,
        BatchProcessor,
        BatchCheckpoint,
        create_batch_from_directory,
        generate_batch_report,
    )
    ```

    Add to __all__ list:
    ```python
        # Batch Processing
        "BatchJob",
        "BatchConfig",
        "BatchResult",
        "BatchProcessor",
        "BatchCheckpoint",
        "create_batch_from_directory",
        "generate_batch_report",
    ```
  </action>
  <verify>python -c "from lib.cinematic import BatchProcessor, ScanImporter, MocapImporter; print('Cinematic exports OK')"</verify>
  <done>cinematic/__init__.py exports batch module</done>
</task>

<task type="auto">
  <name>Update lib/__init__.py version</name>
  <files>lib/__init__.py</files>
  <action>
    Update the version in `lib/__init__.py` to reflect Phase 7.5 completion:

    ```python
    __version__ = "0.4.0"
    __version_info__ = (0, 4, 0)
    ```
  </action>
  <verify>python -c "from lib import __version__; assert __version__ == '0.4.0', f'Expected 0.4.0, got {__version__}'; print('Version OK')"</verify>
  <done>lib/__version__ = "0.4.0"</done>
</task>

<task type="auto">
  <name>Update pyproject.toml version</name>
  <files>pyproject.toml</files>
  <action>
    Read current `pyproject.toml` and update version to 0.4.0:

    Change:
    ```toml
    version = "0.3.4"
    ```
    to:
    ```toml
    version = "0.4.0"
    ```

    This marks MILESTONE v0.6 (Cinematic System) complete.
  </action>
  <verify>grep 'version = "0.4.0"' /Users/bretbouchard/apps/blender_gsd/pyproject.toml && echo "Version OK"</verify>
  <done>pyproject.toml version = "0.4.0"</done>
</task>

<task type="auto">
  <name>Update ROADMAP milestone completion</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Update the ROADMAP.md to mark Phase 7.5 and MILESTONE v0.6 as complete:

    1. Find the MILESTONE v0.6 section
    2. Update status from "In Progress" to "Complete"
    3. Add completion date: **Completed: 2026-02-19**
    4. Ensure Phase 7.5 shows as planned with all 5 plans
  </action>
  <verify>grep -q "v0.6.*Complete" /Users/bretbouchard/apps/blender_gsd/.planning/ROADMAP.md && echo "Milestone OK"</verify>
  <done>ROADMAP.md shows MILESTONE v0.6 complete</done>
</task>

<task type="auto">
  <name>Update STATE.md</name>
  <files>.planning/STATE.md</files>
  <action>
    Update STATE.md to reflect Phase 7.5 planning complete:

    Set current_phase to "7.5-planned"
    Add entry for Phase 7.5 planning session
    Update version to 0.4.0
  </action>
  <verify>grep -q "0.4.0\|7.5" /Users/bretbouchard/apps/blender_gsd/.planning/STATE.md && echo "State OK"</verify>
  <done>STATE.md updated for Phase 7.5</done>
</task>

</tasks>

<verification>
- All Phase 7.5 modules export from tracking/__init__.py
- Batch module exports from cinematic/__init__.py
- Version bumped to 0.4.0 in both lib/__init__.py and pyproject.toml
- ROADMAP.md shows MILESTONE v0.6 complete
- STATE.md reflects Phase 7.5 planning complete
</verification>

<success_criteria>
- All exports accessible from lib.cinematic.tracking
- Version 0.4.0 marks MILESTONE v0.6 (Cinematic System) complete
- Package structure clean and consistent
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-advanced-features/07.5-05-SUMMARY.md`

This completes MILESTONE v0.6 - Cinematic System.
</output>
