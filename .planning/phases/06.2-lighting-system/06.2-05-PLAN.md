---
phase: 06.2-lighting-system
plan: 05
type: execute
wave: 3
depends_on: ["06.2-02", "06.2-03", "06.2-04"]
files_modified:
  - lib/cinematic/__init__.py
autonomous: true
must_haves:
  truths:
    - "All lighting modules are exported from cinematic package"
    - "New types are accessible from lib.cinematic"
    - "Version is updated to reflect lighting system addition"
  artifacts:
    - path: "lib/cinematic/__init__.py"
      provides: "Package exports for lighting system"
      exports: ["create_light", "setup_light_linking", "apply_lighting_rig", "apply_gel", "setup_hdri", "load_hdri_preset"]
      contains: "__version__"
  key_links:
    - from: "lib/cinematic/__init__.py"
      to: "lib/cinematic/lighting.py"
      via: "module import"
      pattern: "from .lighting import"
    - from: "lib/cinematic/__init__.py"
      to: "lib/cinematic/gel.py"
      via: "module import"
      pattern: "from .gel import"
    - from: "lib/cinematic/__init__.py"
      to: "lib/cinematic/hdri.py"
      via: "module import"
      pattern: "from .hdri import"
---

<objective>
Update __init__.py to export all lighting system components.

Purpose: Integrate the lighting, gel, and HDRI modules into the cinematic package's public API. Update version to reflect lighting system addition.

Output: Updated lib/cinematic/__init__.py with all lighting exports and version bump.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference - existing init file
@lib/cinematic/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lighting module imports</name>
  <files>lib/cinematic/__init__.py</files>
  <action>
Update lib/cinematic/__init__.py to add lighting module imports:

1. Add imports from lighting.py:
   ```python
   from .lighting import (
       create_light,
       create_area_light,
       create_spot_light,
       setup_light_linking,
       apply_lighting_rig,
   )
   ```

2. Add imports from gel.py:
   ```python
   from .gel import (
       apply_gel,
       create_gel_from_preset,
       kelvin_to_rgb,
       combine_gels,
   )
   ```

3. Add imports from hdri.py:
   ```python
   from .hdri import (
       setup_hdri,
       load_hdri_preset,
       find_hdri_path,
       clear_hdri,
   )
   ```

Place after existing imports, before __all__ definition.
</action>
  <verify>python3 -c "from lib.cinematic import create_light, apply_gel, setup_hdri; print('imports ok')"</verify>
  <done>Lighting, gel, and HDRI modules imported in __init__.py</done>
</task>

<task type="auto">
  <name>Task 2: Add new types to exports</name>
  <files>lib/cinematic/__init__.py</files>
  <action>
Update the types import to include new dataclasses:

1. Update existing types import:
   ```python
   from .types import (
       Transform3D,
       CameraConfig,
       LightConfig,
       BackdropConfig,
       RenderSettings,
       ShotState,
       PlumbBobConfig,
       RigConfig,
       ImperfectionConfig,
       MultiCameraLayout,
       # New lighting types
       GelConfig,
       HDRIConfig,
       LightRigConfig,
   )
   ```

2. Update enums import:
   ```python
   from .enums import (
       LensType,
       LightType,
       QualityTier,
       ColorSpace,
       EasingType,
       # New enum
       AreaLightShape,
   )
   ```
</action>
  <verify>python3 -c "from lib.cinematic import GelConfig, HDRIConfig, LightRigConfig, AreaLightShape; print('types ok')"</verify>
  <done>New types and enums exported from package</done>
</task>

<task type="auto">
  <name>Task 3: Update __all__ list</name>
  <files>lib/cinematic/__init__.py</files>
  <action>
Update the __all__ list to include all new exports:

Add to existing __all__ list (do not remove existing entries):
```python
    # Lighting
    "create_light",
    "create_area_light",
    "create_spot_light",
    "setup_light_linking",
    "apply_lighting_rig",
    # Gel System
    "apply_gel",
    "create_gel_from_preset",
    "kelvin_to_rgb",
    "combine_gels",
    # HDRI System
    "setup_hdri",
    "load_hdri_preset",
    "find_hdri_path",
    "clear_hdri",
    # New Types
    "GelConfig",
    "HDRIConfig",
    "LightRigConfig",
    "AreaLightShape",
```

Preserve all existing entries in __all__.
</action>
  <verify>python3 -c "from lib.cinematic import __all__; lighting = [x for x in __all__ if 'light' in x.lower() or 'gel' in x.lower() or 'hdri' in x.lower()]; print(len(lighting) >= 10)"</verify>
  <done>__all__ list includes all new exports</done>
</task>

<task type="auto">
  <name>Task 4: Update version number</name>
  <files>lib/cinematic/__init__.py</files>
  <action>
Update the __version__ string to reflect lighting system addition:

Change from current version to:
```python
__version__ = "0.1.2"
```

The version progression:
- 0.1.0 - Foundation (Phase 6.0)
- 0.1.1 - Camera System (Phase 6.1)
- 0.1.2 - Lighting System (Phase 6.2)
</action>
  <verify>python3 -c "from lib.cinematic import __version__; print(__version__ == '0.1.2')"</verify>
  <done>Version updated to 0.1.2</done>
</task>

</tasks>

<verification>
1. All imports work: python3 -c "from lib.cinematic import create_light, apply_gel, setup_hdri, GelConfig, AreaLightShape"
2. Version is 0.1.2: python3 -c "from lib.cinematic import __version__; assert __version__ == '0.1.2'"
3. __all__ contains new exports: python3 -c "from lib.cinematic import __all__; assert 'create_light' in __all__ and 'apply_gel' in __all__ and 'setup_hdri' in __all__"
4. No import errors: python3 -c "import lib.cinematic; print('ok')"
</verification>

<success_criteria>
- All lighting functions exported: create_light, setup_light_linking, apply_lighting_rig
- All gel functions exported: apply_gel, create_gel_from_preset, kelvin_to_rgb, combine_gels
- All HDRI functions exported: setup_hdri, load_hdri_preset, find_hdri_path, clear_hdri
- New types exported: GelConfig, HDRIConfig, LightRigConfig, AreaLightShape
- Version is 0.1.2
- __all__ list updated
- No breaking changes to existing exports
</success_criteria>

<output>
After completion, create `.planning/phases/06.2-lighting-system/06.2-05-SUMMARY.md`
</output>
