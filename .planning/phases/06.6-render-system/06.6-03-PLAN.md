---
phase: 06.6-render-system
plan: 03
type: execute
wave: 3
depends_on: ["06.6-02"]
files_modified:
  - lib/cinematic/__init__.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All render functions are importable from lib.cinematic"
    - "CinematicRenderSettings is exported from package"
    - "Render enums are exported from package"
    - "Preset loader functions for render are exported"
    - "Package version is bumped to 0.2.2"
  artifacts:
    - path: "lib/cinematic/__init__.py"
      provides: "Package exports for render system"
      exports:
        - CinematicRenderSettings
        - RenderEngine
        - DenoiserType
        - EXRCompression
        - apply_quality_tier
        - configure_render_passes
        - configure_exr_output
        - select_denoiser
        - apply_render_config
        - get_render_metadata
        - batch_render
        - get_quality_profile
        - get_pass_preset
        - get_exr_settings
        - list_quality_profiles
        - list_pass_presets
        - list_exr_presets
  key_links:
    - from: "lib/cinematic/__init__.py"
      to: "lib/cinematic/render.py"
      via: "from .render import ..."
      pattern: "from \\.render import"
    - from: "lib/cinematic/__init__.py"
      to: "lib/cinematic/types.py"
      via: "CinematicRenderSettings"
      pattern: "CinematicRenderSettings"
---

<objective>
Export all render system components from lib.cinematic package and bump version

Purpose: Make render system functions and types available to users
Output: Updated __init__.py with ~25 new exports and version 0.2.2
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Current exports
@lib/cinematic/__init__.py

# Prior plan summaries
@.planning/phases/06.6-render-system/06.6-01-SUMMARY.md
@.planning/phases/06.6-render-system/06.6-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Render System Imports and Exports</name>
  <files>lib/cinematic/__init__.py</files>
  <action>
Update lib/cinematic/__init__.py to export all render system components:

1. Add CinematicRenderSettings to the from .types import ( section:
   - Add after RenderSettings in the import block

2. Add render enums to the from .enums import ( section:
   - RenderEngine
   - DenoiserType
   - EXRCompression

3. Add preset loader functions to the from .preset_loader import ( section:
   - get_quality_profile
   - get_pass_preset
   - get_exr_settings
   - list_quality_profiles
   - list_pass_presets
   - list_exr_presets

4. Add new from .render import ( block after motion_path imports:
   - apply_quality_tier
   - configure_render_passes
   - configure_exr_output
   - select_denoiser
   - apply_render_config
   - get_render_metadata
   - batch_render

5. Add all new exports to __all__ list:
   - CinematicRenderSettings
   - RenderEngine
   - DenoiserType
   - EXRCompression
   - get_quality_profile
   - get_pass_preset
   - get_exr_settings
   - list_quality_profiles
   - list_pass_presets
   - list_exr_presets
   - apply_quality_tier
   - configure_render_passes
   - configure_exr_output
   - select_denoiser
   - apply_render_config
   - get_render_metadata
   - batch_render

Follow the exact alphabetical ordering pattern used in the existing __all__ list.

DO NOT remove or modify existing exports. Only add new ones.
  </action>
  <verify>python3 -c "from lib.cinematic import apply_quality_tier, CinematicRenderSettings, RenderEngine, select_denoiser; print('imports ok')"</verify>
  <done>All render system exports available from lib.cinematic package</done>
</task>

<task type="auto">
  <name>Task 2: Update Package Version and Module Docstring</name>
  <files>lib/cinematic/__init__.py</files>
  <action>
Update lib/cinematic/__init__.py version and documentation:

1. Update __version__ from "0.2.1" to "0.2.2"

2. Update module docstring to add render system to the modules list:
   - Add "- render: Render quality tiers, pass configuration, EXR output" after motion_path line

3. Update Quick Start example to include render system:
   - Add after motion_path imports:
   ```python
   # Render system
   CinematicRenderSettings, RenderEngine, DenoiserType,
   apply_quality_tier, configure_render_passes, configure_exr_output,
   select_denoiser, apply_render_config,
   ```

4. Add a render usage example in the Quick Start section:
   ```python
   # Configure render for production
   apply_render_config(
       quality_tier="cycles_production",
       pass_group="full_production",
       exr_preset="multi_layer_compositing"
   )
   ```

DO NOT change the structure of the docstring, just add the new items in the appropriate places.
  </action>
  <verify>python3 -c "from lib.cinematic import __version__; assert __version__ == '0.2.2', f'Version is {__version__}'; print('version ok')"</verify>
  <done>Package version bumped to 0.2.2, documentation updated</done>
</task>

</tasks>

<verification>
1. Verify CinematicRenderSettings is importable
2. Verify all render enums are importable
3. Verify all render functions are importable
4. Verify preset loader functions are importable
5. Verify __version__ is "0.2.2"
6. Verify __all__ list includes all new exports
7. Verify no import errors when running: python3 -c "import lib.cinematic"
</verification>

<success_criteria>
- CinematicRenderSettings exported from package
- RenderEngine, DenoiserType, EXRCompression exported
- All render.py functions exported
- All render preset loaders exported
- __version__ == "0.2.2"
- Module docstring includes render system
- All imports work without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06.6-render-system/06.6-03-SUMMARY.md`
</output>
