---
phase: 06-foundation-cinematic
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - configs/cinematic/cameras/lens_presets.yaml
  - configs/cinematic/cameras/sensor_presets.yaml
  - configs/cinematic/cameras/rig_presets.yaml
  - configs/cinematic/cameras/imperfection_presets.yaml
  - configs/cinematic/lighting/rig_presets.yaml
  - configs/cinematic/lighting/gel_presets.yaml
  - configs/cinematic/lighting/hdri_presets.yaml
  - configs/cinematic/backdrops/infinite_curves.yaml
  - configs/cinematic/backdrops/gradients.yaml
  - configs/cinematic/backdrops/environments.yaml
  - configs/cinematic/color/technical_luts.yaml
  - configs/cinematic/color/film_luts.yaml
  - configs/cinematic/color/creative_luts.yaml
  - configs/cinematic/color/color_management_presets.yaml
  - configs/cinematic/animation/camera_moves.yaml
  - configs/cinematic/animation/easing_curves.yaml
  - configs/cinematic/render/quality_profiles.yaml
  - configs/cinematic/render/pass_presets.yaml
  - configs/cinematic/shots/base/base_product.yaml
  - configs/cinematic/shots/base/base_hero.yaml
  - configs/cinematic/shots/base/base_turntable.yaml
autonomous: true

must_haves:
  truths:
    - "All configuration directories exist under configs/cinematic/"
    - "All YAML files are syntactically valid"
    - "Base shot templates are marked abstract: true"
    - "Preset names follow consistent naming conventions"
  artifacts:
    - path: "configs/cinematic/cameras/lens_presets.yaml"
      provides: "Lens preset definitions"
      contains: "lenses:"
    - path: "configs/cinematic/render/quality_profiles.yaml"
      provides: "Quality tier presets"
      contains: "profiles:"
    - path: "configs/cinematic/shots/base/base_product.yaml"
      provides: "Abstract base template"
      contains: "abstract: true"
  key_links:
    - from: "configs structure"
      to: "lib/cinematic modules"
      via: "YAML preset loading"
      pattern: "Preset files loaded by cinematic modules"
---

<objective>
Create the complete configuration directory structure for the cinematic system.

Purpose: Establish all configuration files that will be loaded by the cinematic modules. These presets define lens configurations, lighting rigs, backdrops, color management, animation moves, render profiles, and shot templates. Having these files in place provides the data that subsequent phases will use.

Output: 7 configuration subdirectories with 21 YAML preset files.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bretbouchard/apps/blender_gsd/.planning/ROADMAP.md
@/Users/bretbouchard/apps/blender_gsd/.planning/phases/06-foundation-cinematic/06-RESEARCH.md
@/Users/bretbouchard/apps/blender_gsd/.planning/design/CINEMATIC_SYSTEM_DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create camera configuration presets</name>
  <files>
    - configs/cinematic/cameras/lens_presets.yaml
    - configs/cinematic/cameras/sensor_presets.yaml
    - configs/cinematic/cameras/rig_presets.yaml
    - configs/cinematic/cameras/imperfection_presets.yaml
  </files>
  <action>
Create the `configs/cinematic/cameras/` directory with 4 YAML preset files.

**1. lens_presets.yaml** - Lens focal length presets (from design document Part 3.2):
```yaml
# Lens Presets
# Focal lengths from 14mm ultra-wide to 135mm telephoto, plus macro and vintage

lenses:
  # Wide angle
  14mm_ultra_wide:
    focal_length: 14
    description: "Dramatic distortion, epic establishing shots"
    distortion: 0.08
    vignette: 0.4
    use_case: establishing

  24mm_wide:
    focal_length: 24
    description: "Environmental context, slight distortion"
    distortion: 0.03
    vignette: 0.2
    use_case: environmental

  # Normal
  35mm_documentary:
    focal_length: 35
    description: "Natural field of view, street feel"
    distortion: 0.01
    vignette: 0.1
    use_case: documentary

  50mm_normal:
    focal_length: 50
    description: "Most natural, human eye equivalent"
    distortion: 0.0
    vignette: 0.05
    use_case: hero

  # Portrait
  85mm_portrait:
    focal_length: 85
    description: "Flattering compression, background separation"
    distortion: 0.0
    vignette: 0.1
    use_case: portrait

  135mm_telephoto:
    focal_length: 135
    description: "Strong compression, isolated subject"
    distortion: 0.0
    vignette: 0.15
    use_case: detail

  # Macro
  90mm_macro:
    focal_length: 90
    description: "Close focus, extreme detail"
    minimum_focus: 0.15
    distortion: 0.0
    use_case: macro

  # Vintage / Character Lenses
  helios_44_2:
    focal_length: 58
    description: "Vintage Soviet, swirly bokeh"
    distortion: 0.02
    vignette: 0.5
    flare: intense
    bokeh_swirl: 0.7
    chromatic_aberration: 0.004
```

**2. sensor_presets.yaml** - Sensor size presets:
```yaml
# Sensor Size Presets
# Standard sensor dimensions for different camera formats

sensors:
  full_frame:
    width: 36.0
    height: 24.0
    description: "Full frame 35mm (most common)"
    crop_factor: 1.0

  aps_c:
    width: 23.5
    height: 15.6
    description: "APS-C (crop sensor)"
    crop_factor: 1.5

  micro_four_thirds:
    width: 17.3
    height: 13.0
    description: "Micro Four Thirds"
    crop_factor: 2.0

  super_35:
    width: 24.89
    height: 18.66
    description: "Super 35 cinema"
    crop_factor: 1.45

  red_monstro:
    width: 40.96
    height: 21.6
    description: "RED Monstro 8K VV"
    crop_factor: 0.88

  arri_alexa_lf:
    width: 36.7
    height: 25.54
    description: "ARRI Alexa LF"
    crop_factor: 0.98
```

**3. rig_presets.yaml** - Camera rig presets (from design document Part 3.3):
```yaml
# Camera Rig Presets
# Motion control systems for camera movement

rigs:
  tripod:
    description: "Static with pan/tilt"
    degrees_of_freedom: [pan, tilt]
    constraints:
      position_locked: true

  tripod_orbit:
    description: "Orbit around plumb bob"
    motion:
      type: orbit
      axis: z
      range: [0, 360]

  dolly:
    description: "Linear track movement"
    track:
      type: linear
      direction: forward
    wheels: false

  dolly_curved:
    description: "Curved track for arc shots"
    track:
      type: bezier
      control_points: auto

  crane:
    description: "3D arc movement"
    arm_length: 3.0
    max_height: 5.0
    motion:
      type: arc_3d

  steadicam:
    description: "Smooth handheld simulation"
    smoothing:
      position: 0.8
      rotation: 0.9
    human_motion: true

  drone:
    description: "Free-flight path following"
    motion:
      type: path
      speed: constant
    constraints: none
```

**4. imperfection_presets.yaml** - Lens character presets:
```yaml
# Lens Imperfection Presets
# Realistic lens character for cinematic look

imperfections:
  cooke_s4:
    description: "Cooke S4 - Warm, organic character"
    flare:
      enabled: true
      intensity: 0.3
      streaks: 6
    vignette: 0.15
    chromatic_aberration: 0.001
    bokeh_shape: rounded

  arri_master_prime:
    description: "ARRI Master Prime - Clean, sharp"
    flare:
      enabled: true
      intensity: 0.15
      streaks: 8
    vignette: 0.08
    chromatic_aberration: 0.0005
    bokeh_shape: circular

  vintage_helios:
    description: "Helios 44-2 - Swirly bokeh, heavy flare"
    flare:
      enabled: true
      intensity: 0.6
      streaks: 16
    vignette: 0.4
    chromatic_aberration: 0.004
    bokeh_swirl: 0.7

  clean:
    description: "Minimal imperfections"
    flare:
      enabled: false
    vignette: 0.0
    chromatic_aberration: 0.0
```
  </action>
  <verify>
```bash
# Verify camera config files exist and are valid YAML
for f in configs/cinematic/cameras/*.yaml; do
  python3 -c "import yaml; yaml.safe_load(open('$f')); print('OK: $f')"
done

# Verify expected content
python3 -c "
import yaml

# Check lens presets has expected lenses
lenses = yaml.safe_load(open('configs/cinematic/cameras/lens_presets.yaml'))
assert '85mm_portrait' in lenses['lenses']
assert lenses['lenses']['85mm_portrait']['focal_length'] == 85
print('Lens presets validated')

# Check sensor presets
sensors = yaml.safe_load(open('configs/cinematic/cameras/sensor_presets.yaml'))
assert 'full_frame' in sensors['sensors']
print('Sensor presets validated')
"
```
  </verify>
  <done>4 camera config files exist with valid YAML, lens_presets has 8 lenses, sensor_presets has 6 sensors</done>
</task>

<task type="auto">
  <name>Task 2: Create lighting configuration presets</name>
  <files>
    - configs/cinematic/lighting/rig_presets.yaml
    - configs/cinematic/lighting/gel_presets.yaml
    - configs/cinematic/lighting/hdri_presets.yaml
  </files>
  <action>
Create the `configs/cinematic/lighting/` directory with 3 YAML preset files.

**1. rig_presets.yaml** - Lighting rig presets (from design document Part 4.2):
Include at minimum: three_point_soft, three_point_hard, product_hero, product_dramatic, studio_high_key, studio_low_key, console_overhead, mixer_angle

**2. gel_presets.yaml** - Color correction gels:
Include: CTO (Color Temperature Orange), CTB (Color Temperature Blue), diffusion grades (quarter, half, full), creative colors

**3. hdri_presets.yaml** - HDRI environment presets (from design document Part 4.3):
Include: studio_bright, studio_soft, overcast_day, golden_hour, night_city

Reference the design document sections 4.2 and 4.3 for complete configuration details.
  </action>
  <verify>
```bash
# Verify lighting config files exist and are valid YAML
for f in configs/cinematic/lighting/*.yaml; do
  python3 -c "import yaml; yaml.safe_load(open('$f')); print('OK: $f')"
done

# Verify rig presets has expected rigs
python3 -c "
import yaml
rigs = yaml.safe_load(open('configs/cinematic/lighting/rig_presets.yaml'))
assert 'three_point_soft' in rigs['rigs']
assert 'product_hero' in rigs['rigs']
print('Lighting rigs validated')
"
```
  </verify>
  <done>3 lighting config files exist with valid YAML, rig_presets has 8+ rigs</done>
</task>

<task type="auto">
  <name>Task 3: Create backdrop configuration presets</name>
  <files>
    - configs/cinematic/backdrops/infinite_curves.yaml
    - configs/cinematic/backdrops/gradients.yaml
    - configs/cinematic/backdrops/environments.yaml
  </files>
  <action>
Create the `configs/cinematic/backdrops/` directory with 3 YAML preset files.

**1. infinite_curves.yaml** (from design document Part 5.2):
Include: white_studio, gray_studio, dark_studio, gradient_warm, gradient_cool

**2. gradients.yaml**:
Linear, radial, and angular gradient definitions with color stops

**3. environments.yaml**:
Pre-built environment/scene references
  </action>
  <verify>
```bash
# Verify backdrop config files exist and are valid YAML
for f in configs/cinematic/backdrops/*.yaml; do
  python3 -c "import yaml; yaml.safe_load(open('$f')); print('OK: $f')"
done
```
  </verify>
  <done>3 backdrop config files exist with valid YAML</done>
</task>

<task type="auto">
  <name>Task 4: Create color configuration presets</name>
  <files>
    - configs/cinematic/color/technical_luts.yaml
    - configs/cinematic/color/film_luts.yaml
    - configs/cinematic/color/creative_luts.yaml
    - configs/cinematic/color/color_management_presets.yaml
  </files>
  <action>
Create the `configs/cinematic/color/` directory with 4 YAML preset files.

**1. technical_luts.yaml** (from design document Part 6.1):
Include: rec709, srgb, p3_dci, aces_cg

**2. film_luts.yaml**:
Include: kodak_2383, fuji_3510, cineon

**3. creative_luts.yaml**:
Cinematic looks and artistic grades

**4. color_management_presets.yaml** (from design document Part 13.20):
Include: srgb_standard, agx_default, agx_product, agx_dramatic, acescg, filmic
  </action>
  <verify>
```bash
# Verify color config files exist and are valid YAML
for f in configs/cinematic/color/*.yaml; do
  python3 -c "import yaml; yaml.safe_load(open('$f')); print('OK: $f')"
done

# Verify color management presets
python3 -c "
import yaml
presets = yaml.safe_load(open('configs/cinematic/color/color_management_presets.yaml'))
assert 'agx_default' in presets['presets']
print('Color management presets validated')
"
```
  </verify>
  <done>4 color config files exist with valid YAML, color_management_presets has agx_default</done>
</task>

<task type="auto">
  <name>Task 5: Create animation configuration presets</name>
  <files>
    - configs/cinematic/animation/camera_moves.yaml
    - configs/cinematic/animation/easing_curves.yaml
  </files>
  <action>
Create the `configs/cinematic/animation/` directory with 2 YAML preset files.

**1. camera_moves.yaml** (from design document Part 7.1):
Include: orbit_90, orbit_180, orbit_360, dolly_in, dolly_out, push_in, rack_focus, crane_up, reveal

**2. easing_curves.yaml**:
Define: linear, ease_in, ease_out, ease_in_out with mathematical descriptions
  </action>
  <verify>
```bash
# Verify animation config files exist and are valid YAML
for f in configs/cinematic/animation/*.yaml; do
  python3 -c "import yaml; yaml.safe_load(open('$f')); print('OK: $f')"
done
```
  </verify>
  <done>2 animation config files exist with valid YAML</done>
</task>

<task type="auto">
  <name>Task 6: Create render configuration presets</name>
  <files>
    - configs/cinematic/render/quality_profiles.yaml
    - configs/cinematic/render/pass_presets.yaml
  </files>
  <action>
Create the `configs/cinematic/render/` directory with 2 YAML preset files.

**1. quality_profiles.yaml** (from design document Part 13.1):
Include all 5 tiers: viewport_capture, eevee_draft, cycles_preview, cycles_production, cycles_archive
Each tier must specify: engine, resolution (base, scale), samples, adaptive_sampling settings

CRITICAL: Use `BLENDER_EEVEE_NEXT` for eevee_draft, NOT the deprecated `BLENDER_EEVEE`

**2. pass_presets.yaml** (from design document Part 13.2):
Include pass groups: beauty, data_passes (depth, normal, vector), material_passes (diffuse, glossy, transmission), cryptomatte
  </action>
  <verify>
```bash
# Verify render config files exist and are valid YAML
for f in configs/cinematic/render/*.yaml; do
  python3 -c "import yaml; yaml.safe_load(open('$f')); print('OK: $f')"
done

# Verify quality profiles has all tiers
python3 -c "
import yaml
profiles = yaml.safe_load(open('configs/cinematic/render/quality_profiles.yaml'))
expected = ['viewport_capture', 'eevee_draft', 'cycles_preview', 'cycles_production', 'cycles_archive']
for tier in expected:
    assert tier in profiles['profiles'], f'Missing tier: {tier}'
print('All 5 quality tiers validated')
"
```
  </verify>
  <done>2 render config files exist with valid YAML, quality_profiles has all 5 tiers</done>
</task>

<task type="auto">
  <name>Task 7: Create base shot template files</name>
  <files>
    - configs/cinematic/shots/base/base_product.yaml
    - configs/cinematic/shots/base/base_hero.yaml
    - configs/cinematic/shots/base/base_turntable.yaml
  </files>
  <action>
Create the `configs/cinematic/shots/base/` directory with 3 abstract base template files.

These templates follow the shot template inheritance pattern from design document Part 1.9.

**1. base_product.yaml**:
```yaml
name: base_product
abstract: true

camera:
  lens:
    preset: 50mm_normal
    aperture: f/5.6

lighting:
  rig: product_hero

backdrop:
  type: infinite_curve
  preset: gray_studio

color:
  look:
    preset: kodak_2383
    intensity: 0.5
```

**2. base_hero.yaml**:
```yaml
name: base_hero
abstract: true

camera:
  lens:
    preset: 85mm_portrait
    aperture: f/4

lighting:
  rig: three_point_soft

backdrop:
  type: infinite_curve
  preset: white_studio

color:
  view_transform: AgX Default Medium High Contrast
```

**3. base_turntable.yaml**:
```yaml
name: base_turntable
abstract: true

camera:
  lens:
    preset: 50mm_normal
    aperture: f/8

animation:
  turntable:
    enabled: true
    subject_rotation: true
    duration: 120

lighting:
  rig: three_point_soft

backdrop:
  type: infinite_curve
  preset: gray_studio
```

CRITICAL: All base templates must have `abstract: true` to prevent direct rendering.
  </action>
  <verify>
```bash
# Verify shot template files exist and are valid YAML
for f in configs/cinematic/shots/base/*.yaml; do
  python3 -c "
import yaml
data = yaml.safe_load(open('$f'))
assert data.get('abstract') == True, 'Base template must be abstract'
print('OK: $f (abstract=True)')
"
done
```
  </verify>
  <done>3 base shot template files exist with valid YAML, all marked abstract: true</done>
</task>

</tasks>

<verification>
Verify the complete configuration directory structure:

```bash
# Count total config files
file_count=$(find configs/cinematic -name "*.yaml" | wc -l | tr -d ' ')
echo "Total YAML files: $file_count"
# Expected: 21 files

# Verify each subdirectory exists
dir_count=$(ls -d configs/cinematic/*/ | wc -l | tr -d ' ')
echo "Subdirectories: $dir_count"
# Expected: 7 directories

# Validate all YAML syntax
invalid=0
for f in $(find configs/cinematic -name "*.yaml"); do
  python3 -c "import yaml; yaml.safe_load(open('$f'))" || invalid=$((invalid+1))
done
echo "Invalid YAML files: $invalid"
# Expected: 0

# Summary
echo ""
echo "Configuration Structure:"
find configs/cinematic -type d | sort | while read dir; do
  count=$(ls "$dir"/*.yaml 2>/dev/null | wc -l | tr -d ' ')
  echo "  $dir: $count files"
done
```
</verification>

<success_criteria>
- configs/cinematic/ directory exists with 7 subdirectories
- cameras/ contains 4 YAML files (lens, sensor, rig, imperfection presets)
- lighting/ contains 3 YAML files (rig, gel, hdri presets)
- backdrops/ contains 3 YAML files (curves, gradients, environments)
- color/ contains 4 YAML files (technical, film, creative luts, color management)
- animation/ contains 2 YAML files (camera moves, easing curves)
- render/ contains 2 YAML files (quality profiles, pass presets)
- shots/base/ contains 3 abstract base templates
- Total of 21 YAML configuration files
- All YAML files are syntactically valid
- All base templates have `abstract: true`
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundation-cinematic/06-02-SUMMARY.md`
</output>
